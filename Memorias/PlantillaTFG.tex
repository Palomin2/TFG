%&pdflatex
%!TEX TS-program = pdflatex
%!TEX encoding = UTF-8 Unicode
%
% PLANTILLA PARA UN TRABAJO DE FIN DE GRADO
% EN EL GRADO DE MATEMÁTICAS DE LA UNIVERSIDAD DE ZARAGOZA
%
% Mario Pérez Riera
% Área de Análisis Matemático, Universidad de Zaragoza
%
% Última modificación: 1 de agosto de 2021
%
% Escrito para pdfLaTeX
%
%Imporatante para la bilbiografia: bibtex PlantillaTFG
\documentclass[11pt]{book}

% Paquetes y fuentes
%---------------------------------------------------------------------------
\usepackage[dvipsnames, table, xcdraw]{xcolor}
\usepackage{geometry}               % define las dimensiones de la página
\geometry{
 a4paper, % Tamaño del papel
 centering, % Zona útil centrada
 margin = 25mm, % Márgenes en blanco: 25mm
% includehead, % Incluir la cabecera en el área del texto
% includefoot, % Incluir el pie en el área del texto
}
\usepackage[utf8]{inputenc}
\usepackage[numbers, sort&compress]{natbib}
\setlength{\bibsep}{10pt}
%\usepackage[natbibapa]{apacite}
%\usepackage{natbib}                % para usar el teclado normalmente
\usepackage[spanish]{babel}        % selecciona idioma
\usepackage[T1]{fontenc}           % gestión de fuentes con acentos
\usepackage{graphicx}
\usepackage{svg}
\usepackage{subcaption}
\usepackage{float}
\usepackage{caption}
\usepackage{listings}
\usepackage{listingsutf8}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

\usepackage{booktabs}
\usepackage{array}
\definecolor{ventaja}{RGB}{235, 255, 235} 
\definecolor{desventaja}{RGB}{255, 245, 235} 

\usepackage{minted}
\usemintedstyle{colorful}

% Si alguno de estos paquetes no los vas a usar, mejor los quitas:
\usepackage{amssymb}               % para cargar las fuentes de la A.M.S.
\usepackage{amsmath}               % para usar letras griegas en negrita
\usepackage{amsthm}                % mejora los entornos tipo-teorema y proof
\usepackage{mathptmx}              % fuente Times
%\usepackage{lmodern}              % fuente Times
\usepackage{tikz}                  % para los dibujos
\usetikzlibrary{babel}              % para evitar conflictos entre TikZ y babel

%\usepackage{longtable}             % permite saltos de páginas en tablas largas

%------ Enlaces en el pdf
\usepackage{url}
\usepackage[pdftex, colorlinks=true, citecolor=cyan, linkcolor=black, 
urlcolor=black]{hyperref}

%------ Cabeceras y pies de página
\usepackage{titleps}

\newpagestyle{miestilo}{
%-- Cabecera par y cabecera impar
\sethead[\thepage][][\textsl{\chaptername\ \thechapter. \chaptertitle}]
{\textsl{Análisis isogeométrico \ - \ Carlos Palomera Oliva}}{}{\thepage}
%-- Pie de página par y pie de página impar
%\setfoot[][\textsl{\thesection. \sectiontitle}][]
%{}{\textsl{\thesection. \sectiontitle}}{}
}

\newpagestyle{primeraparte}{
%-- Cabecera par y cabecera impar
\sethead[\thepage][][\textsl{\chaptertitle}]
{\textsl{Análisis isogeométrico \ - \ Carlos Palomera Oliva}}{}{\thepage}
%-- Pie de página par y pie de página impar
%\setfoot[][\textsl{\thesection. \sectiontitle}][]
%{}{\textsl{\thesection. \sectiontitle}}{}
}

%------ \S 4.3, p. 93, de The LaTeX Companion 1a ed. (Figura 4.2) (Juan Luis)
\newcommand{\clearemptydoublepage}{\newpage{\pagestyle{empty}\cleardoublepage}}
\newcommand{\sectionmarkwithoutsections}[1]{\markright{#1}}

\makeindex

\theoremstyle{plain} % Entornos tipo teorema
\newtheorem{teorema}{Teorema}[chapter]
\newtheorem{proposicion}[teorema]{Proposición}
%\newtheorem{lema}[teorema]{Lema} % La misma numeración que 'teorema'

\theoremstyle{definition} % Entornos tipo definición
\newtheorem*{defin}{Definición} % El asterisco hace que no se numere
%\newtheorem{ejemplo}{Ejemplo}

\theoremstyle{definition} % Entornos tipo definición
\newtheorem{lemma}{Lema}[section] 

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codeblue}{rgb}{0,0,0.8}

\lstdefinestyle{miC++}{
    language=C++,
    basicstyle=\small\ttfamily,
    keywordstyle=\color{codeblue}\bfseries,
    commentstyle=\color{codegreen},
    stringstyle=\color{red},
    numberstyle=\tiny\color{codegray},
    breaklines=true,
    captionpos=b,
    frame=tb, % Marco arriba y abajo
    tabsize=2
}

%------ Conjuntos de números
\newcommand\N{\mathbb{N}}
\newcommand\Z{\mathbb{Z}}
\newcommand\R{\mathbb{R}}
\newcommand\C{\mathbb{C}}

%------ Funciones en español
\renewcommand{\spanishoperators}{Re}
%\renewcommand{\spanishoperators}{Re ctg arc\,ctg} % Define \Re, \ctg, \arctg

%-------------
\begin{document}
%-------------
\frontmatter                     % Parte primera del texto
\pagestyle{primeraparte}    % Estilo de cabeceras para la parte primera del texto

%\pagenumbering{roman} % Incluido en \frontmatter

%--- PORTADA DEL TFG
\begin{titlepage}
\begin{center}

\null
\vfill

\Huge{\bfseries Análisis}\\%
\Huge{\bfseries isogeométrico}

\vfill

\noindent
\begin{figure}[h]
\centering
\includegraphics[width=100mm]{ciencias.png}\\%
\includegraphics[width=100mm]{logoUZ.png}
\end{figure}

\vfill

\Huge{\bfseries Carlos Palomera Oliva}\\%
\Huge{Trabajo de fin de grado de Matemáticas}\\%
\Huge{Universidad de Zaragoza}\\%

\vfill
\huge{Noviembre de 2025}
\end{center}
\end{titlepage}
%--- FIN DE LA PORTADA

\clearemptydoublepage

%--- RESUMEN
\chapter{Summary}

Se creará al finalizar el trabajo.
%--- FIN DEL RESUMEN

\clearemptydoublepage

\tableofcontents

\clearemptydoublepage

%-------------
\mainmatter                      % Parte principal del texto
\pagestyle{miestilo}    % Estilo de cabeceras para la parte principal del texto

%--- CAPÍTULO 1 DEL TFG
\chapter{Introducción}

\section{Contexto y Relación con el Método de Elementos Finitos}

El análisis isogeométrico (IGA) es un método numérico, cuya finalidad se basa en la resolución de problemas de ecuaciones en derivadas parciales.

Se asemeja al método de elementos finitos (FEM), ambos buscan la solución mediante una combinación lineal de funciones base a partir de la formulación variacional ,su principal diferencia con él radica en la preservación de la geometría original del espacio en el que se trabaja.

\begin{table}[htbp]
    \centering
    \caption{Comparación técnica entre FEM e IGA.}
    \label{tab:fem_vs_iga}
    \renewcommand{\arraystretch}{1.5}
    \begin{tabular}{p{3cm} p{5.5cm} p{5.5cm}}
        \toprule
        \textbf{Criterio} & \textbf{FEM Clásico} & \textbf{Análisis Isogeométrico (IGA)} \\
        \midrule
        
        \textbf{Geometría} & 
        \cellcolor{desventaja} \textbf{Aproximada.} Malla poligonal que introduce error geométrico. & 
        \cellcolor{ventaja} \textbf{Exacta.} Geometría NURBS idéntica al CAD. \\
        \midrule
        
        \textbf{Funciones de Base} & 
        Interpolatorias (Nodos). & 
        NURBS (Puntos de Control). \\
        \midrule
        
        \textbf{Continuidad} & 
        \cellcolor{desventaja} Baja ($C^0$). & 
        \cellcolor{ventaja} Alta ($C^{p-1}$). \\
        \midrule
        
        \textbf{Refinamiento} & 
        $h$ y $p$. & 
        \cellcolor{ventaja} $h$, $p$ y \textbf{$k$-refinamiento}. \\
        \midrule
        
        \textbf{Coste Ensamblaje} & 
        \cellcolor{ventaja} \textbf{Económico.} Cuadratura sencilla. & 
        \cellcolor{desventaja} \textbf{Elevado.} Requiere reglas de cuadratura mas complejas. \\
        \midrule
        
        \textbf{Mapeo Inverso} & 
        \cellcolor{ventaja} \textbf{Directo.} Inversión simple o analítica.  & 
        \cellcolor{desventaja} \textbf{Complejo.} aumento en el coste computacional. \\
        \bottomrule
    \end{tabular}
\end{table}
\textit{Nota:} Si se desea conocer mas en detalle los distintos elementos del FEM para comparar más meticulosamente ambos métodos refierase al anexo \ref{FEM}.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.32\textwidth}
        \includesvg[width=\linewidth]{bases_fem_p1.svg}
        \caption{Funciones base FEM, p=1.}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.33\textwidth}
        \includesvg[width=\linewidth]{bases_fem_p2.svg}
        \caption{Funciones base FEM, p=2.}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\textwidth}
        \includesvg[width=\linewidth]{funciones_base_NURBS_2.svg}
        \caption{Funciones base NURBS, p=2.}
    \end{subfigure}
    %\caption{Código para las figuras en el anexo}
    % --- NOTA DEL CÓDIGO FUENTE ---
    \vspace{0.5em} % Pequeño espacio vertical para separar de las subfiguras
    \begin{flushleft}
        \footnotesize{\textit{Nota}: El código fuente de estas figuras se encuentra disponible en los anexos \ref{sec:codigo_FEM} y \ref{sec:codigo_NURBS1}.}
    \end{flushleft}
    % -------------------------------
\end{figure}


\section{Evolución del Análisis Isogeométrico}

Para comprender el IGA, es necesario comprender la evolución del diseño asistido por ordenador (CAD), en \textbf{1967} Ahlberg, Nilson y Walsh publican su obra \textit{Theory of Splines and Their Applications},
pionera en formalizar la teoría de los \textbf{splines}.

Con la llegada de la industria de automoción y aeronáutica en los 70 y 80, comienza la necesidad de representar formas complejas de manera eficiente impulsando así el CAD.

Durante esta época se estandarizaron las técnicas y los algoritmos esenciales que posteriormente serían las bases del IGA.

Paralelamente a este proceso, en los años 50 y 60, impulsado por la industria aeroespacial, con el fin de resolver ecuaciones en derivadas parciales en dominios complejos, surgió el FEM, ignorando el CAD y creando una brecha entre ellos que duró décadas.

En una búsqueda de unificar el diseño asistido por ordenador (CAD) y el análisis asistido por ordenador (CAE), tratando de eliminar la necesidad de crear una conversión geométrica que afecta a la precisión, nace el análisis isogeométrico.

Actualmente IGA se usa en diferenctes industrias como: \textbf{automoción},  simulación de choques, \textbf{aeroespacial}, diseño de álabes de turbinas, \textbf{biomedicina}, simulación de flujo sanguíneo, \textbf{eólica}, Interacción fluido-estructura en las palas.

Finalmente mencionamos los campos de estudio en los que se avanzan actualmente en referencia a IGA como son los \textbf{T-Splines}, para un mayor refinamiento local, y los métodos \textbf{espacio-temporales}, campo actualmente en investigación.

\section{Objetivos}

El objetivo principal del trabajo es entender el marco teórico y las ventajas del IGA frente al FEM, tanto en costo computacional como en orden de convergencia, en problemas de contorno de ecuaciones en derivadas parciales.

Nos centraremos el estudio de los casos 1D y 2D, pues la generalización a dimensiones superiores es análoga.

Con el fin de cumplir este objetivo, este se segmentará en los siguientes puntos que se seguiran en el orden siguiente:

\begin{enumerate}
    \item \textbf{Marco teórico:} Establecer el marco teórico del IGA, contrastando la formulación variacional basada en las funciones NURBS con la aproximación polinómica del FEM.
    \item \textbf{Implementación computacional:} Desarrollo e implementación de código propio que pueda resolver porblemas de contorno de edps usando el IGA.
    \item \textbf{Validación y comparación:} Validar los resultados numéricos comparandolos con las soluciones analíticas de los problemas y comprobar las diferencias en velocidad de convergencia con el FEM.
\end{enumerate}

\section{El Problema Modelo: la Ecuación de Poisson}

Durante la realización del trabajo nos centraremos en el problema de Poisson como benchmark para mostrar en este las propiedades del IGA.

\subsection{Formulación Fuerte (o Clásica)}
\begin{defin} Sea $\Omega \subset \mathbb{R}^n$ un conjunto abierto y acotado, donde $n \in \mathbb{N}$,
     con frontera $\partial\Omega = \Gamma = \overline{\Gamma_D \cup \Gamma_R \cup \Gamma_N}$, que sea suficientemente regular, tal que $\Gamma_D \cap \Gamma_R = \emptyset$, $ \Gamma_D \cap \Gamma_N = \emptyset$, $  \Gamma_R \cap \Gamma_N = \emptyset$ .

Buscamos una función solución $u: \Omega \to \mathbb{R}$, que cumpla $u \in C^2(\Omega)$ y tal que: 

\[
\begin{cases}
- \Delta u = f & \text{en } \Omega, \\[6pt]
u = g & \text{en } \Gamma_D, \quad \text{(Condición de Dirichlet)}, \\[6pt]
\nabla u \cdot \mathbf{n} = h & \text{en } \Gamma_N, \quad \text{(Condición de Neumann)}, \\[6pt]
\alpha u + \beta \, \nabla u \cdot \mathbf{n} = r & \text{en } \Gamma_R, \quad \text{(Condición de Robin)}.
\end{cases}
\]

Donde las formas bilineal $a(\cdot,\cdot)$ y lineal $L(\cdot)$ se definen como:

\begin{itemize}
    \item ${f: \Omega \to \mathbb{R}}$, $f \in L^2(\Omega)$.
    \item ${g: \Gamma_D \to \mathbb{R}}$, $g \in H^{1/2}(\Gamma_D)$.
    \item ${h: \Gamma_N \to \mathbb{R}}$, $h \in L^2(\Gamma_N)$.
    \item ${r: \Gamma_R \to \mathbb{R}}$, $r \in L^2(\Gamma_R)$.
    \item ${\mathbf{n}: \Gamma \to \mathbb{R}^n}$, vector normal exterior unitario a la frontera $\Gamma$.
    \item ${\alpha, \beta \in \mathbb{R}}$, tal que $\frac{\alpha}{\beta} \geq 0$.
\end{itemize}
\end{defin}

\subsection{Formulación Variacional (o Débil)}\label{sec:Debil}
\begin{defin} Buscamos  $u \in V(\Omega)=\{v \in H^1(\Omega) \mid v=g \text{ en } \Gamma_D\}$, tal que para  
$v \in V_0(\Omega)=\{v \in H^1(\Omega) \mid v=0 \text{ en } \Gamma_D\}$, se cumple que:

\begin{equation}
    \label{eq:form_weak}
    a(u,v)=L(v).
\end{equation}

Donde:
\begin{itemize}
    \item ${a(u,v)}=\int_{\Omega}\nabla u \cdot \nabla v\, d\Omega + \int_{\Gamma_R}\frac{\alpha}{\beta}uv\, d\Gamma$.
    \item ${L(v)}=\int_{\Omega}fv \, d\Omega+\int_{\Gamma_N}hv\, d\Gamma+\int_{\Gamma_R}\frac{r}{\beta}v\, d\Gamma$.
\end{itemize}
\end{defin}

\textit{Nota:} El desarrollo detallado para obtener \eqref{eq:form_weak} a partir de la formulación fuerte, la demostración de existencia y unicidad de la solución variacional, así como la prueba de que esta (bajo suficiente regularidad) es también solución del problema fuerte, se detallan en los Anexos \ref{sec:Paso_Soluciones}, \ref{sec:Lax} y \ref{sec:equivform}.
%nota aqui sobre que las demostraciones de la equivalencia de las soluciones y del paso a la formulación clasica a variacional se encuentran en el anexo x e y

\section{Estructura del Documento}

resumen rapido de los posteriores capitulos asi como explicación de donde apareceran el codigo o diferentes elementos en el anexo.

Se creará al finalizar el trabajo.
%--- FIN DEL CAPÍTULO 1

\clearemptydoublepage

%--- CAPÍTULO 2 DEL TFG
\chapter{Construcción de Estructuras mediante CAD}


%Se mencionara rapidamente conceptos como el paso de formulación fuerte a formulación débil o variacional, las funciones base polinómicas clásicas con su espacio de funciones y la suavidad que estas aportan, así como su convergencia en L2 y H1.



%En este apartado no sé si explicar tambíen las curvas/superficies de Bezier y/o los B-splines como inicio o si quedaría demasiado largo así. 
%Se seguira en este apartado el libro de NURBS para explicar como realizar tanto las estructuras simples aportando ejemplos de estas variando su suavidad a lo largo de su parametrizacion, se introducirá también el concepto isoparamétrico y como se refina el mallado mediante inserción de nodos.

Las curvas y superficies creadas mediante \textbf{NURBS} son la base y el estándar del diseño CAD, durante esta sección introduciremos los conceptos necesarios para entender su funcionamiento e implementación siguiendo la progresión natural e histórica de las siguientes curvas: 
\begin{equation*}
    \text{Bézier} \quad \longrightarrow \quad \text{B-Splines} \quad \longrightarrow \quad \text{NURBS}.
\end{equation*}
\section{Bézier}

En los años 60, impulsados por la industria de la automoción francesa y su necesidad de pasar los diseños en arcilla y papel a ordenador. Pierre Bézier y Paul Casteljau desarrollan la manera de crear curvas parametrizadas mediante el uso de \textbf{polinomios de Bernstein} como funciones base y \textbf{puntos de conttrol}.

\subsection{Funciones Base}
\begin{defin}
Los polinomios de Bernstein se definen como: $$B_{i,p}(u) = \binom{p}{i} u^{i} (1-u)^{p-i}.$$
\end{defin}

\begin{proposicion}
    Algunas de las propiedades fundamentales de los \textbf{polinomios de Bernstein} en este contexto son:
    \begin{itemize}
        \item \textbf{No Negatividad}: $B_{i,p}(u) \geq 0, \forall u \in [0,1]$.
        \item \textbf{Partición de la Unidad}: $\sum_{i=0}^{p} B_{i,p}(u) = 1, \quad \forall u \in [0,1]$.
        \item \textbf{Interpolación de los extremos}: $B_{0,p}(0) = 1, \quad B_{p,p}(1) = 1$.
        \item \textbf{Soporte Global}: $B_{i,p}(u) > 0, \quad \forall u \in (0,1)$.
    \end{itemize}
    \begin{proof}
        Demostraciones omitidas temporalmente.
    \end{proof}
\end{proposicion}

\subsection{Curvas}
\begin{defin}
    Las curvas de Bézier de grado $p$ son curvas parametrizadas en el espacio $\R^n$ con $n,p \in \N$, de la forma:
    \begin{equation*}
        \mathbf{C}(u) = \sum_{i=0}^{p} B_{i,p}(u) \mathbf{P}_i, \quad u \in [0, 1].
    \end{equation*}
    Donde:
    \begin{itemize}
        \item $\{\mathbf{P}_0, \mathbf{P}_1, \dots , \mathbf{P}_p\} \footnote{El conjunto de puntos de control puede contener repeticiones, es decir, \textbf{NO} satisfacen necesariamente $\mathbf{P}_i \neq \mathbf{P}_j, \quad \text{si } i \neq j$.}\subset \R^n$, es el conjunto formado por los \textbf{puntos de control}.
    \end{itemize}
\end{defin}

\begin{proposicion}
    Sea $\mathbf{C}(u) = \sum_{i=0}^{p} B_{i,p}(u)\mathbf{P}_i$ una curva de Bézier de grado $p$, con $u \in [0,1]$. Se satisfacen las siguientes propiedades:

    \begin{enumerate}
        \item \textbf{Interpolación en los extremos}:
         $\mathbf{C}(0) = \mathbf{P}_0$ y $\mathbf{C}(1) = \mathbf{P}_p$.
        \item \textbf{Tangencia en los extremos}:
        \begin{equation*}
            \mathbf{C}'(0) = p(\mathbf{P}_1 - \mathbf{P}_0), \quad \mathbf{C}'(1) = p(\mathbf{P}_p - \mathbf{P}_{p-1}).
        \end{equation*}
        \textit{Implicación:} La continuidad $C^1$ entre dos curvas de Bézier unidas requiere la colinealidad de $\mathbf{P}_{p-1}, \mathbf{P}_p$ (primera curva) y $\mathbf{P}_1$ (segunda curva).
        
        \item \textbf{Invarianza afín}:
        Sea $\Phi: \mathbb{R}^n \to \mathbb{R}^n$ una transformación afín ( $\Phi(\mathbf{x}) = \mathbf{A}\mathbf{x} + \mathbf{b}$). Entonces:
        \begin{equation*}
            \Phi\left( \sum_{i=0}^{p} B_{i,p}(u)\mathbf{P}_i \right) = \sum_{i=0}^{p} B_{i,p}(u)\Phi(\mathbf{P}_i).
        \end{equation*}
    \end{enumerate}
    \begin{proof}
        Demostraciones omitidas temporalmente.
    \end{proof}
\end{proposicion}

Uno de los principales inconvenientes de las curvas de Bézier es su incapacida de representar cónicas de forma exacta, para ello se introduce el concepto de racionalizarlas.

\begin{proposicion} Racionalización de las curvas de Bézier.

    Se le asigna un peso $w_i\in \R^+$ a cada punto de control $P_i$, de manera que elevamos cada uno a coordenadas homogéneas en el \textbf{espacio proyectivo $\mathbb{P}^n$
    \footnote{Sea $\sim$ una relación de equivalencia, donde $\mathbf{X} \sim \mathbf{Y}$ si $\mathbf{Y} = \lambda \mathbf{X}$, con $\lambda \in \mathbb{R} \setminus \{0\}$. El \textbf{espacio proyectivo} se define como $\mathbb{P}^n = \left( \mathbb{R}^{n+1} \setminus \{\mathbf{0}\} \right) / \sim$.}} y proyectando de vuelta al espacio euclídeo $\R^n$.
    
    
    \begin{equation*}
        \mathbf{C}^R(u) = \frac{\sum_{i=0}^{p} B_{i,p}(u) w_i \mathbf{P}_i}{\sum_{i=0}^{p} B_{i,p}(u) w_i}
    \end{equation*}
\end{proposicion}
\begin{figure}[H]
    \centering
    \begin{subfigure}{0.32\textwidth}
        \includesvg[width=\linewidth]{curva_bezier.svg}
        \caption{Curva Bézier no racional.}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.33\textwidth}
        \includesvg[width=\linewidth]{curva_bezier_racional.svg}
        \caption{Curva racional, $W=(1,10,7,1)^T$.}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\textwidth}
        \includesvg[width=\linewidth]{bernstein_bases_p3.svg}
        \caption{Funciones base de Bernstein.}
    \end{subfigure}
    %\caption{Código para las figuras en el anexo}
    % --- NOTA DEL CÓDIGO FUENTE ---
    \vspace{0.5em} % Pequeño espacio vertical para separar de las subfiguras
    \begin{flushleft}
        \footnotesize{\textit{Nota}: El código fuente para la generación de estas figuras se encuentra disponible en el anexo \ref{sec:codigo_bezier}.}
    \end{flushleft}
    % -------------------------------
\end{figure}

\section{B-Splines}
A pesar de que la racionalización de las curvas de Bézier permitió la representación de cónicas exactas, hubo otros problemas intrínsecos a estas que motivaron la transición en los años 70 debido a la necesidad de representar estructuras mas complejas en la industria aeroespacial y de automoción,
se transiciona entonces a la implementacion de funciones base \textbf{B-spline} que permitian, debido a tener un soporte local y un número de elemtos no ligado obligatoriamente al grado polinomial de estas, superar las limitaciones de las funciones base de Bernstein.

%Debido a las limitaciones que tienen las curvas de Bézier, como pueden ser el requirimiento de un orden polinomial $n-1$ para $n$ puntos de control, aperece en este contexto los \textbf{B-Splines}.
\subsection{Funciones Base}

\begin{defin}
    El \textbf{Vector de nudos} se define como $$U = \{u_0, u_1, \ldots, u_m\},$$ cumplen $u_i \le u_{i+1}, \quad i = 0, \ldots, m-1$, el número de nudos $m+1$ está relacionado con $p$ y $n$ tal que cumple: $m=n+p+1$.

    A lo largo del trabajo nos enfocaremos en los vectores de nudos \textbf{abiertos}, es decir, aquellos cuyos extremos se repiten $p+1$ veces, $u_0=u_1=\dots=u_{p}$, $u_m=u_{m-1}=\dots=u_{m-p+1}$.
\end{defin}

\begin{defin}
    Las \textbf{Funciones base B-Spline, $N_{i,p}(u)$}: se definen recursivamente mediante el \textbf{algoritmo de Cox-de Boor}:
                \begin{itemize}
                    \item Caso base ($p=0$): 
                    \begin{equation*}
                        N_{i,0}(u) = \begin{cases} 1 & \text{si } u_i \le u < u_{i+1}, \\ 0 & \text{en caso contrario.} \end{cases}
                    \end{equation*} 
                    \item Caso genérico ($p\geq 1$):
                    \begin{equation*}
                        N_{i,p}(u) = \frac{u - u_i}{u_{i+p} - u_i} N_{i,p-1}(u) + \frac{u_{i+p+1} - u}{u_{i+p+1} - u_{i+1}} N_{i+1,p-1}(u) \footnote{Para el caso en el que los coeficientes toman valor $\frac{0}{0}$ se les asignará como valor 1.}.
                    \end{equation*}
                \end{itemize} 
    
\end{defin}

\begin{proposicion}
    Algunas de las propiedades importantes de las funciones base B-Splines son:
    \label{prop:bspline_basis_fundamentales}
    \begin{enumerate}
        \item \textbf{Soporte Local}: Cada función base $N_{i,p}(u)$ es no nula solo en un número limitado de intervalos de nudo, concretamente en $[u_i, u_{i+p+1})$. En consecuencia la curva adquiere la propiedad de \textbf{control local}: mover un punto de control $\mathbf{P}_i$ solo altera la forma de la curva en una región limitada.
        
        \item \textbf{No Negatividad}: El valor de las funciones base B-Spline es siempre no negativo en todo el dominio paramétrico, es decir, $\forall u \in [u_0, u_m]$, $N_{i,p}(u) \geq 0$.


        \item \textbf{Número de Bases Activas}:  Para cualquier intervalo de nudos $[u_i, u_{i+1})$, las únicas funciones base $N_{j,p}(u)$ cuyo soporte contiene dicho intervalo son aquellas con el índice:
        \begin{equation*}
            j \in \{i-p, i-p+1, \ldots, i\}
        \end{equation*}
        
        Por tanto el número de funciones base no nulas en el intervalo es a lo sumo $p+1$.

        El número exacto de funciones base no nulas decrece con la multiplicidad $k$ de $u_i$, reduciendose a $\mathbf{p+1-k}$.

        \item \textbf{Partición de la Unidad}: $\forall u\in [u_0, u_m]$, se cumple,
        $\sum_{i=0}^{n} N_{i,p}(u) = 1$.
    \end{enumerate}
    \begin{proof}
        La demostración puede encontrarse en el anexo \ref{sec:B-Splines_proof}.
    \end{proof}
\end{proposicion}
\subsection{Curvas}

\begin{defin}
    Con estas definiciones podemos ahora definir la curva generada por los B-Splines:
    \begin{equation*}
        C(u) = \sum_{i=0}^{n} N_{i,p}(u) \cdot \mathbf{P}_i.
    \end{equation*}
\end{defin}

\begin{proposicion}
    \label{prop:bspline_curvas_fundamentales}
    Algunas de las propiedades importantes de las curvas B-Splines son:
    \begin{enumerate}
        \item \textbf{Invarianza bajo Transformaciones Afines}: La aplicación de una transformación afín al polígono de control resulta en la misma transformación aplicada directamente a la curva generada.
        
        \item \textbf{Continuidad Controlable}: La continuidad de la curva en cualquier unión (nudo) se controla localmente mediante la multiplicidad $k$\footnote{La multiplicidad $k$ de un nudo $u_i$ es el número de veces que el valor $u_i$ aparece en el vector de nudos $U$, debe cumplirse que $k\leq p$ en los nudos interiores.} del nudo, sin depender de la manipulación compleja de los puntos de control. La continuidad es $C^r$, donde $r = p - k$.
    \end{enumerate}
    \begin{proof}
        La demostración puede encontrarse en el anexo \ref{sec:B-Splines_proof}.
    \end{proof}
\end{proposicion}
\section{NURBS}
%Introducimos finalmente la extensión racional de los B-Splines, los \textbf{NURBS}, quedando la curva generada por estos definida de la siguiente manera,
La transición de \textbf{B-splines} a \textbf{NURBS} no fue instantánea, se tuvo que esperar hasta \textbf{1979}, donde Boeing sufría las consecuencias de no poder representar cónicas exactas mediante B-splines, teniendo así su software
segmentado, usando un programa basado en B-splines para el fuselaje y otro basado en las \textbf{cónicas de Rho} para las partes cónicas, siendo incapaces de conectarlos sin perder precisión.

En este caos Boeing inicia el proyecto \textbf{TIGER}, reescribiendo así todo su sistema CAD interno basándose en la tesis de \textbf{Verspille}, proponiendo así en \textbf{1981} a la IGES que las \textbf{NURBS} sean el estandar universal, transicionando así la industria de manera natural entre \textbf{1983} y \textbf{1990}.

\subsection{Funciones Base}
\begin{defin}
    Las \textbf{Funciones base NURBS} se definen como:
    \begin{equation}
        \label{eq:NURBS_Curve_Basis}
        R_i^p(u)=\frac{N_{i,p}(u) w_i}{\sum_{j=0}^{n} N_{j,p}(u) w_j}.
    \end{equation}
\end{defin}

\begin{proposicion}
    Con el fin de preservar todas las propiedades de las \textbf{funciones base B-spline} dadas en \ref{prop:bspline_basis_fundamentales}, nos restringiremos al caso $w_i>0, \forall i$.
\end{proposicion}

\subsection{Curvas}
\begin{defin}
    Podemos ahora definir las curvas generadas por NURBS como:

    \begin{equation}
        \label{eq:NURBS_Curve}
        \mathbf{C}(u) = \sum_{i=0}^{n} R_i^p(u) \cdot \mathbf{P}_i = \frac{\sum_{i=0}^{n} N_{i,p}(u) w_i \mathbf{P}_i}{\sum_{j=0}^{n} N_{j,p}(u) w_j}.
    \end{equation}

\end{defin}

\begin{proposicion}
    Debido a la restricción impuesta anteriormente ($w_i>0, \forall i$), las \textbf{curvas NURBS} también mantienen las propiedades de las \textbf{curvas B-spline} dadas en \ref{prop:bspline_curvas_fundamentales}.
\end{proposicion}
%Mantiene todas las propiedades de los B-Splines definidas en \ref{prop:bspline_basis_fundamentales}.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.32\textwidth}
        \includesvg[width=\linewidth]{CurvaNurbs_3D_Plot.svg}
        \caption{Curva NURBS.}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.33\textwidth}
        \includesvg[width=\linewidth]{funciones_base_NURBS.svg}
        \caption{funciones base $R_{i,p}$.}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\textwidth}
        \includesvg[width=\linewidth]{Ders_funciones_base_NURBS.svg}
        \caption{Derivadas de  $R_{i,p}$.}
    \end{subfigure}
    %\caption{Código para las figuras en el anexo}
    % --- NOTA DEL CÓDIGO FUENTE ---
    \vspace{0.5em} % Pequeño espacio vertical para separar de las subfiguras
    \begin{flushleft}
        \footnotesize{\textit{Nota}: El código fuente para la generación de estas figuras se encuentra disponible en el anexo \ref{sec:codigo_NURBS1}.}
    \end{flushleft}
    % -------------------------------
\end{figure}

\section{Creación de Superficies (Producto Tensorial)}
El producto tensorial es el estándar de CAD para la creación de superficies, se define como:
\begin{equation*}
    \mathbf{B}_{i,j}(u, v) = N_{i,p}(u) \cdot M_{j,q}(v).
\end{equation*}
Donde  $N_{i,p}(u)$ son las funciones base en la dirección de $u$ y $M_{j,q}(v)$ son las funciones base en la dirección de $v$.

Mediante este producto definimos la superficie generada por \textbf{B-Splines} mediante la fórmula:
\begin{equation*}
    \mathbf{S}(u, v) = \sum_{i=0}^{n} \sum_{j=0}^{m} N_{i,p}(u) M_{j,q}(v) \mathbf{P}_{i,j}.
\end{equation*}
Redefiniendo la estructura en la que se encuentran los puntos de control, pasando a formar ahora una matriz.

Podemos ahora racionalizar estas curvas para obtener superficies generadas mediante \textbf{NURBS}, para ello utilizamos una matriz de pesos $w_{i,j}$ y la fórmula:
\begin{equation}
    \label{eq:NURBS_Surf}
    \mathbf{S}(u, v) = \frac{\sum_{i=0}^{n} \sum_{j=0}^{m} N_{i,p}(u) M_{j,q}(v) \mathbf{w}_{i,j} \mathbf{P}_{i,j}}{\sum_{i=0}^{n} \sum_{j=0}^{m} N_{i,p}(u) M_{j,q}(v) \mathbf{w}_{i,j}}= \sum_{i=0}^{n} \sum_{j=0}^{m} R_{i,j}^{p,q}(u, v) \mathbf{P}_{i,j}.
\end{equation}

Donde 
\begin{equation} 
    \label{eq:NURBS_Surf_Basis}
    R_{i,j}^{p,q}(u, v) = \frac{N_{i,p}(u) M_{j,q}(v) w_{i,j}}{\sum_{k=0}^{n} \sum_{l=0}^{m} N_{k,p}(u) M_{l,q}(v) w_{k,l}} 
\end{equation}

\begin{proposicion}
    \label{prop:surf_tensor}
    Las superficies generadas mediante el prodcuto tensorial mantienen la suavidad de las curvas que la generan en las direcciones de estas.
    \begin{proof}
        Tomaremos una dirección arbitraria, $v$ en este caso, y fijaremos $u=u_0$, la demostración es análoga para la dirección $u$.
        \begin{equation*}
            \mathbf{S}(u_0, v) = \frac{\sum_{i=0}^{n} \sum_{j=0}^{m} N_{i,p}(u_0) M_{j,q}(v) \mathbf{w}_{i,j} \mathbf{P}_{i,j}}{\sum_{i=0}^{n} \sum_{j=0}^{m} N_{i,p}(u_0) M_{j,q}(v) \mathbf{w}_{i,j}}.
        \end{equation*}
        Ahora reagrupamos los valores que dependen de $v$ y los que no:
        \begin{equation*}
            \mathbf{S}(u_0, v) = \frac{\sum_{j=0}^{m} M_{j,q}(v) \left( \sum_{i=0}^{n} N_{i,p}(u_0) \mathbf{w}_{i,j} \mathbf{P}_{i,j} \right)}{\sum_{j=0}^{m} M_{j,q}(v) \left( \sum_{i=0}^{n} N_{i,p}(u_0) \mathbf{w}_{i,j} \right)}.
        \end{equation*}
        Definimos ahora los siguientes elementos:
        \begin{equation*}
            \mathbf{w}^*_j=\sum_{i=0}^n  N_{i,p}(u_0) \mathbf{w}_{i,j}, \quad  \mathbf{Q}_j=\frac{\sum_{i=0}^n N_{i,p}(u_0) \mathbf{P}_{i,j} \mathbf{w}_{i,j}}{\mathbf{w}^*_j}.
        \end{equation*}
        Obteniendo al final una definición 
        \begin{equation*}
            C(v)=S(u_0,v)=\frac{\sum_{j=0}^{m} M_{j,q}(v) \mathbf{w}^*_{j} \mathbf{Q}_j }{\sum_{j=0}^{m} M_{j,q}(v) \mathbf{w}^*_{j} }.
        \end{equation*}
        Obteniendo así la curva de NURBS asociada a la dirección $v$ en el punto $u_0$.
    \end{proof}
\end{proposicion}

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.32\textwidth}
        \includesvg[width=\linewidth]{Superficie_NURBS_Ej1.svg}
        \caption{Superficie NURBS ejemplo 1.}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.33\textwidth}
        \includesvg[width=\linewidth]{Superficie_NURBS_Ej2.svg}
        \caption{Superficie NURBS ejemplo 2.}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\textwidth}
        \includesvg[width=\linewidth]{Superficie_NURBS_Ej2_Refined.svg}
        \caption{Ejemplo 2 refininada.}
    \end{subfigure}
    % --- NOTA DEL CÓDIGO FUENTE ---
    \vspace{0.5em} 
    \begin{flushleft}
        \footnotesize{\textit{Nota}: El código fuente para la generación de estas figuras se encuentra disponible en el anexo \ref{sec:codigo_NURBS2}.}
    \end{flushleft}
    % -------------------------------
\end{figure}

\section{Refinamiento de Nudos (h-Refinement)}
En las figuras anteriores podemos observar un refinamiento de la superfice del ejemplo 2, este procedimiento se basa en insertar nuevos valores en un vector de nudos, introduciendo nuevos puntos de control y pesos para mantener la misma figura geométrica.

El refinamiento de nudos tiene como finalidad aumentar el control local donde se añade el nudo, obteniendo un mayor número de funciones base donde se inserta y decreciendo el tamaño de los elementos sobre los que se trabaja. 

\begin{defin}
        Definiremos los elementos que necesitamos para aplicar el refinamiento:
        \begin{itemize}
            \item \textbf{Puntos de control ponderados}, $\mathbf{P}_i^w$: Sea $\mathbf{P}_i$ un punto de control y $\mathbf{w}_i$ su peso asociado, definimos $\mathbf{P}_i^w$ = ($\mathbf{w}_i\mathbf{P}_i, \mathbf{w}_i)^T$.
            \item \textbf{Nuevo nudo}, $\xi$: aunque ya tenemos el concepto de nudo definido es necesario declarar en que intervalo se encuentra, tomaremos que $\xi \in [u_k, u_{k+1})$.
            \item \textbf{Nuevo vector de nodos}, $U^*=\{u_0,u_1, \dots , u_k, \xi, u_{k+1}, \dots, u_m\}$.
            \item \textbf{Nuevos puntos de control ponderados}, $\mathbf{Q}_i^w$: estrán definidos mediante $\mathbf{Q}_{i}^{w} = \alpha_i \mathbf{P}_{i}^{w} + (1 - \alpha_i) \mathbf{P}_{i-1}^{w}$, donde $\alpha_i$ se define mediante la fórmula:
                $$\alpha_i = \begin{cases} 1 & \text{si } i \le k-p, \\ \frac{\xi - u_i}{u_{i+p} - u_i} & \text{si } k-p+1 \le i \le k, \\ 0 & \text{si } i > k. \end{cases}$$
            \item \textbf{Nuevas funciones base}, $N_{i,p}^*(u)$, asociadas al nuevo vector de nodos $U^*$, que cumplen la igualdad: $N_{i,p}(u) = \alpha_{i} N_{i,p}^*(u) + (1 - \alpha_{i+1}) N_{i+1,p}^*(u)$\footnote{Demostración en el anexo \ref{sec:dem_new_bases}}.
        \end{itemize}
\end{defin}
\begin{proposicion}
    La curva original ponderada es equivalente a la curva ponderada refinada,
    \begin{equation*}
        \mathbf{C}^{w}(u) = \sum_{i=0}^{m} N_{i,p}(u) \mathbf{P}_{i}^{w}=  \sum_{j=0}^{m+1} N_{j,p}^*(u) \mathbf{Q}_{j}^{w} 
    \end{equation*}
    \begin{proof}
        La demostración puede encontrarse en el anexo \ref{sec:refinement_proof}.
    \end{proof}
\end{proposicion}

La inserción de nudos en superficies se realiza de manera direccional, introduciendo unicamente elementos a uno de los dos vectores de nudos,
 modificando así una de las dos curvas que generan la superficie, pero al ser invariante bajo la inserción de nudos esta, la superficie generada final es identica a la orginal,
  los pesos y puntos de control en forma matricial son tratados como en la demostración de la proposición \ref{prop:surf_tensor}, trabajando con ellos de forma vectorial 
  ,relacionandolos con el vector de nudos que recibe la inserción.

\subsection{Implementación de la Inserción de Nudos}

Durante el trabajo se utilizaran las siguientes funciones para la inserción de nudos en las estructuras correspondientes:

\paragraph{Caso curvas (1D):}
La función \texttt{CurveKnotsIns} calcula los nuevos puntos de control tras insertar un nudo $u$ con multiplicidad $r$.

\begin{lstlisting}[style=miC++, caption={Parámetros para la función de inserción de nudos en curvas.}, label={lst:curve_ins}]
iMatrix<double> CurveKnotsIns(
    int np,                         // n (num. ptos control)
    int p,                          // p (grado)
    std::vector<double> &KnotVector,// Vector de Nudos (U)
    const iMatrix<double> &CtrlPtsW,// Ptos Control Ponderados (Pw)
    double u,                       // Valor del nudo a insertar
    int index,                      // Span index (k)
    int s,                          // Multiplicidad inicial
    int r                           // Veces a insertar
);
\end{lstlisting}

\paragraph{Caso superficies (2D):}
Para superficies NURBS, la función se generaliza para operar sobre las direcciones $u$ (índice 1) o $v$ (índice 2) según el parámetro booleano \texttt{dir}.

\begin{lstlisting}[style=miC++, caption={Parámetros para la función de inserción de nudos en superficies.}, label={lst:surf_ins}]
std::vector<iMatrix<double>> SurfaceKnotIns(
    int np1, int p1, std::vector<double> &KnotVector1, // Dir U
    int np2, int p2, std::vector<double> &KnotVector2, // Dir V
    const std::vector<iMatrix<double>> &CtrlPtsW,      // Red de control
    bool dir,       // true = Dir U, false = Dir V
    double uv,      // Valor del nudo a insertar
    int index,      // Span index
    int r,          // Veces a insertar
    int s           // Multiplicidad inicial
);
\end{lstlisting}

La correspondencia matemática de los parámetros es:
\begin{itemize}
    \item \texttt{np, np1, np2}: Corresponden al número de puntos de control ($n$).
    \item \texttt{CtrlPtsW}: Representa los puntos de control en coordenadas homogéneas $\mathbf{P}^w_{i,j} = (w_ix_i, w_iy_i, w_iz_i, w_i)$.
    \item \texttt{index}: Corresponde al índice del intervalo nodal o \textit{span} donde se encuentra el nudo a insertar.
\end{itemize}

El código fuente de estas implementaciones puede encontrarse en el anexo \ref{sec:codigo_fund}.
%--- FIN DEL CAPÍTULO 2

\clearemptydoublepage

%--- CAPÍTULO 3 DEL TFG
\chapter{Análisis Isogeométrico}

Durante el capítulo definiremos las bases necesarias para el IGA y profundizaremos en estas, luego sobre esta base desarrollaremos la teoría necesaria para resolver el problema de Poisson.

\section{Espacios Funcionales}





\begin{defin}    
    Sea $\hat{\Omega}$ el \textbf{dominio paramétrico} definido en 1D por el vector de nodos $U$ y en 2D por el producto tensorial de los vectores de nodos $U \times V$.
\end{defin}



El \textbf{espacio físico} $\Omega$ queda parametrizado así mediante el mapeo geométrico $\mathbf{G}: \hat{\Omega} \to \Omega$ definido en \ref{eq:NURBS_Curve} en 1D y en \ref{eq:NURBS_Surf} en 2D.

\begin{defin}
    \textbf{Elemento:} denotado por $\Omega_e\subset \Omega$, parametrizado mediante la restricción sobre $\hat{\Omega}$ de $\mathbf{G}$, dada por el intervalo no nulo $[u_n, u_{(n+1)})$ en 1D, o por $[u_n, u_{(n+1)})\times [v_k, v_{(k+1)})$ en 2D, 
\end{defin}

\begin{defin}
    \textbf{Malla física:} Sea $N_{el}$ el número de intervalos de nudos no nulos formados por nudos consecutivos, definimos la malla física como $\mathcal{T}_h = \{ \Omega_e \}_{e=1}^{N_{el}}$.
\end{defin}

\begin{defin}
    \textbf{Diámetro de un elemento:} $h_e = \text{diam}(\Omega_e) = \sup_{\mathbf{x}, \mathbf{y} \in \Omega_e} \| \mathbf{x} - \mathbf{y} \|_2$.
\end{defin}

\begin{defin}
    \textbf{Parámetro de discretización global:} $h = \max_{e} h_e$
\end{defin}



\begin{defin}
    Sea $\hat{V}_h$ el \textbf{espacio de funciones NURBS} definido en 1D en \ref{eq:NURBS_Curve_Basis} y en 2D en \ref{eq:NURBS_Surf_Basis}

    Definimos ahora el \textbf{espacio de aproximación} $V_h$ como
    \begin{equation*}
        V_h = \Big\{ v : \Omega \to \mathbb{R} \mid v \circ \mathbf{G} \in \hat{V}_h \Big\}.
    \end{equation*}
\end{defin}

\begin{defin}
    Las \textbf{funciones de forma} $\phi_i$ en 1D y  $\phi_{i,j}$ en 2D se definen como:\footnote{A lo largo de esta sección y las siguientes, todas aquellas funciones que tomen como dominio a $\hat{\Omega}$ serán diferenciadas mediante la imposición del acento circunflejo en estas ($\hat{\cdot}$).} 
    \begin{align*}
        \phi_i &= \hat{R}_i^p \circ \mathbf{G}^{-1}.\\
        \phi_{i,j} &= \hat{R}_{i,j}^{p,q} \circ \mathbf{G}^{-1}.
    \end{align*}
\end{defin}

\section{Formulación Discreta de Galerkin}

Partiendo del espacio de aproximación $V_h \subset V$ (mismo V que en la sección \ref{sec:Debil}), aplicamos el principio de Galerkin al problema variacional (\ref{sec:Debil}). Buscamos una solución aproximada $u_h \in V_h$ tal que:
\begin{equation} \label{eq:Galerkin_Form}
    a(u_h, v_h) = L(v_h), \quad \forall v_h \in V_h.
\end{equation}

Expresamos la solución discreta $u_h$ como una combinación lineal de las funciones de forma definidas anteriormente:
\begin{equation*}
    u_h(\mathbf{x}) = \sum_{j=1}^{N_b} d_j \phi_j(\mathbf{x}),
\end{equation*}
donde $d_j$ son los \textbf{coeficientes de control} a determinar y $N_b$ es el número total de funciones base globales.

Sustituyendo $u_h$ en la ecuación \ref{eq:Galerkin_Form} y tomando sucesivamente cada función de base como función de test ($v_h = \phi_i$ para $i=1,\dots,N_b$), obtenemos el sistema lineal de ecuaciones:
\begin{equation*}
    \sum_{j=1}^{N_b} a(\phi_j, \phi_i) d_i = L(\phi_i), \quad \forall i=1,\dots,N_b.
\end{equation*}

Reescribiendo ahora en forma matricial como:
\begin{equation*}
    \mathbf{K} \mathbf{d} = \mathbf{F},
\end{equation*}
donde $\mathbf{K}\in \mathbb{R}^{N_b \times N_b}$ es la matriz de rigidez global con elementos $K_{i,j} = a(\phi_j, \phi_i)$, $\mathbf{d}$ es el vector de coeficientes de control a determinar y $\mathbf{F}$ es el vector de carga con componentes $F_i = L(\phi_i)$.

\section{Propiedades del sistema}
\begin{proposicion}
    

Las siguientes propiedades se cumplen para problemas en un abierto $\Omega \subset \R^n$, con $N_{el}$ elementos.

\begin{enumerate}
    \item \textbf{Dipsersión:} cada fila tiene a lo sumo $(2p+1)^n$, el cual no crece en función del número de elementos del problema, creando la relación asitótica
    \begin{equation*} 
        \lim_{N_{el} \to \infty} \frac{(2p+1)^n}{N_B} = 0.
    \end{equation*}
    \item \textbf{Simetría:} los elementos $\mathbf{K}_{(i,j)}=a(\phi_j,\phi_i)=a(\phi_i,\phi_j)=\mathbf{K}_{(j,i)}$, esta propiedad ocurre si y solo la forma bilineal es simétrica, que depende de que el operador diferencial del problema sea autoadjunto.
    \item \textbf{Condicionamiento:} $\kappa(\mathbf{K}) \approx O(h^{-2})$
\end{enumerate}
\begin{proof}
    Omitida temporalmente.
\end{proof}
\end{proposicion}

\section{Caso 1D}


\subsection{Elementos de la matriz global y el vector de carga}
Transformamos la \textbf{forma bilineal} basada en las funciones de forma en la siguiente integral basada en las funciones base NURBS,

\begin{align*}
    &a(\phi_j(\mathbf{x}),\phi_i(\mathbf{x})) = \int_{\Omega}\nabla \phi_j(\mathbf{x}) \cdot \nabla \phi_i(\mathbf{x}) \, d\Omega + \int_{\Gamma_R}\frac{\alpha}{\beta}\phi_j(\mathbf{x})\phi_i(\mathbf{x}) \, d\Gamma =\\[10pt]
    &= \int_{\hat{\Omega}} \left( \frac{d\hat{R}_{j}^p(\xi)}{d\xi} J^{-1}(\xi) \right) \left( \frac{d\hat{R}_{i}^p(\xi)}{d\xi} J^{-1}(\xi) \right) |\det J(\xi)| \, d\hat{\Omega} + \left[ \frac{\alpha}{\beta}\hat{R}_{j}^p(\xi)\hat{R}_{i}^p(\xi) \right]_{\xi \in \hat{\Gamma}_R}= \\[10pt]
    &= \int_{\hat{\Omega}} \frac{d\hat{R}_{j}^p(\xi)}{d\xi} \frac{d\hat{R}_{i}^p(\xi)}{d\xi} \frac{1}{|\det J(\xi)|} \, d\hat{\Omega} + \left[ \frac{\alpha}{\beta}\hat{R}_{j}^p(\xi)\hat{R}_{i}^p(\xi) \right]_{\xi \in \hat{\Gamma}_R}.
\end{align*}

Aplicamos lo mismo a la \textbf{forma lineal},

\begin{align*}
    &L(\phi_i(\mathbf{x})) = \int_{\Omega}f(\mathbf{x})\phi_i(\mathbf{x}) \, d\Omega + \int_{\Gamma_N}h(\mathbf{x})\phi_i(\mathbf{x}) \, d\Gamma + \int_{\Gamma_R}\frac{r}{\beta}\phi_i(\mathbf{x}) \, d\Gamma =\\[10pt]
    &= \int_{\hat{\Omega}}f(\mathbf{G}(\xi))\hat{R}_i^p(\xi)|\det J(\xi)| \, d\hat{\Omega} + \left[ h(\mathbf{G}(\xi))\hat{R}_i^p(\xi) \right]_{\xi \in \hat{\Gamma}_N} + \left[ \frac{r}{\beta}\hat{R}_i^p(\xi) \right]_{\xi \in \hat{\Gamma}_R}.
\end{align*}

\subsection{Cálculo de las integrales}

El cálculo de las integrales definidas anteriormente se realizará elemento a elemento, entendiendo por elementos los intervalos no nulos, $\hat{\Omega}_e=[u_n, u_{n+1})$.

Dentro de cada elemento se calcularán todas las combinaciones de funciones base activas, que gracias a la teoría de NURBS y por estar las funciones de forma directamente relacionadas con estas, sabemos que hay a lo sumo $p+1$ funciones base activas en cada elemento, obteniendo así una \textbf{matriz local} $\mathbf{K}^e$ de tamaño $(p+1) \times (p+1)$.

Teniendo ahora los elementos de esta matriz la forma,

\enlargethispage{3\baselineskip}

\begin{equation*}
    \mathbf{K}_{i,j}^e=\int_{u_n}^{u_{n+1}} \frac{d\hat{R}_{(j+ n - p - 1)}^p(\boldsymbol{\xi})}{d\xi}\cdot \frac{d\hat{R}_{(i+ n - p - 1)}^p(\boldsymbol{\xi})}{d\xi} \frac{1}{|\det J|} \, d\xi.
\end{equation*}

Para formalizar el proceso de ensamblaje, definimos la \textbf{matriz de rigidez local extendida}, denotada por $\tilde{\mathbf{K}}^e \in \mathbb{R}^{N_b \times N_b}$, la cual se construye explícitamente mediante la asignación

\begin{equation*}
    \tilde{\mathbf{K}}^e_{(i + n - p - 1,  j + n - p - 1)} = \mathbf{K}_{i,j}^e,\quad \forall i,j \in \{1,2, \dots, p+1\}, \footnote{En la práctica estas matrices no existirán, se sumarán directamente los elementos de $\mathbf{K}^e$ en $\mathbf{K}$.}
\end{equation*}

siendo cero cualquier otro elemento de $\tilde{\mathbf{K}}^e$ cuyos índices no satisfagan esta correspondencia.

Finalmente la matriz global $\mathbf{K}$ se obtiene al sumar estas matrices extendidas
\begin{equation*}
    \mathbf{K} = \sum_{e=1}^{N_{el}} \tilde{\mathbf{K}}^e.
\end{equation*}

Aplicamos el mismo procedimiento ahora a la forma lineal, definimos el \textbf{vector de carga local}  $\mathbf{F}^e \in \R^{(p+1)}$ cuyos elementos definimos,

\begin{equation*}
    \mathbf{F}^e_i=\int_{u_n}^{u_{n+1}}f(\mathbf{G}(\xi))R_{(i+ n - p - 1)}^p(\xi)|det J|d\xi.
\end{equation*}

Definimos el \textbf{vector de carga local extendido} $\tilde{\mathbf{F}}^e_i \in \R^{N_b}$, construido mediante la signación

\begin{equation*}
    \tilde{\mathbf{F}}^e_{(i+ n - p - 1)}=\mathbf{F}^e_i, \quad \forall i \in \{ 1,2, \dots ,p+1 \},\footnote{Al igual que en las matrices extendidas anteriores, estos vectores no existirán en la práctica y los elementos de $\mathbf{F}^e$ serán ensamblados directamente sobre $\mathbf{F}$.}
\end{equation*}

siendo cero cualquier otro elemento de $\tilde{\mathbf{F}}^e$ que no satisfaga la correspondencia.

Para finalizar se obtiene el vector global $\mathbf{F}$ al sumar todos los vectores extendidos

\begin{equation*}
    \mathbf{F} = \sum_{e=1}^{N_{el}} \tilde{\mathbf{F}}^e.
\end{equation*}

\subsection{Imposición de condiciones de contorno.}
Tras el ensamblado de la matriz de rigidez global y del vector de carga se implementan las condiciones de contorno,

Al ser en el caso 1D, solo es posible aplicar condiciones de contorno a dos puntos del dominio, explicaremos como se aplican al punto $u_0$ que será análogo para el punto $u_m$.


\begin{flushleft}
    \footnotesize{\textit{Nota}: La expliación de por qué se realizan así las imposiciones de contorno esta omitida temporalmente.}
\end{flushleft}

\subsubsection{Tipo Dirichlet}

Sustituimos la primera (última en el caso $u_m$) fila de $\mathbf{K}$, el elemento (1,1) será 1, y el resto pasarán a ser 0, $\mathbf{F}_1$ tomará el valor de la condición de contorno $g(u_0)$.

Con el fin de mantener la simetría del sistema se modificarán las $p$ filas siguientes (anteriores), estableceremos a 0 los elementos de la forma (1,j), y modificaremos los elementos $\mathbf{F}_j$ restandoles el valor $\mathbf{K}_{(1,j)}g(u_0)$.

\subsubsection{Tipo Neumann}

Actualizaremos el valor de $\mathbf{F}_1\rightarrow \mathbf{F}_1+h(u_0)$  ($\mathbf{F}_{N_b} \rightarrow \mathbf{F}_{N_b}+h(u_m)$).

\subsubsection{Tipo Robin o Mixtas}

Actualizaremos tanto $\mathbf{K}_{(1,1)}\rightarrow \mathbf{K}_{(1,1)} + \frac{\alpha}{\beta}$, $\left( \mathbf{K}_{(N_b,N_b)}\rightarrow \mathbf{K}_{(N_b,N_b)} + \frac{\alpha}{\beta}\right)$ y $\mathbf{F}_1 \rightarrow \mathbf{F}_1 + \frac{r}{\beta}$, $\left( \mathbf{F}_{N_b} \rightarrow \mathbf{F}_{N_b} + \frac{r}{\beta} \right)$.

\section{Caso 2D}

\subsection{Elementos de la matriz global y el vector de carga}

\begin{align*}
    &a(\phi_{(i_2,j_2)}(\mathbf{x}),\phi_{(i_1,j_1)}(\mathbf{x})) = \int_{\Omega}\nabla \phi_{(i_2,j_2)}(\mathbf{x}) \cdot \nabla \phi_{(i_1,j_1)}(\mathbf{x}) \, d\Omega \nonumber + \int_{\Gamma_R}\frac{\alpha}{\beta}\phi_{(i_2,j_2)}(\mathbf{x})\phi_{(i_1,j_1)}(\mathbf{x}) \, d\Gamma =\\[10pt]
    &= \int_{v_0}^{v_{m_v}}\int_{u_0}^{u_{m_u}} \left( \mathbf{J}^{-T}(\xi,\eta) \hat{\nabla} \hat{R}_{(i_2,j_2)}^{(p,q)}(\xi,\eta) \right)^T \cdot \left( \mathbf{J}^{-T}(\xi,\eta) \hat{\nabla} \hat{R}_{(i_1,j_1)}^{(p,q)}(\xi,\eta) \right) |\det \mathbf{J}(\xi,\eta)| \, d\xi d\eta \nonumber \\
    &\quad + \int_{\hat{\Gamma}_R}\frac{\alpha}{\beta}\hat{R}_{(i_2,j_2)}^{(p,q)}(\xi,\eta)\hat{R}_{(i_1,j_1)}^{(p,q)}(\xi,\eta) J|_{\hat{\Gamma}}(\xi,\eta) \, d\hat{\Gamma} =\\[10pt]
    &= \int_{v_0}^{v_{m_v}}\int_{u_0}^{u_{m_u}} \left(\hat{\nabla}\hat{R}_{(i_2,j_2)}^{(p,q)}(\xi,\eta)\right)^T \left( \mathbf{J}^{-1}(\xi,\eta) \mathbf{J}^{-T}(\xi,\eta) \right) \left(\hat{\nabla}\hat{R}_{(i_1,j_1)}^{(p,q)}(\xi,\eta)\right) |\det \mathbf{J}(\xi,\eta)| \, d\xi d\eta \nonumber \\
    &\quad + \int_{\hat{\Gamma}_R}\frac{\alpha}{\beta}\hat{R}_{(i_2,j_2)}^{(p,q)}(\xi,\eta)\hat{R}_{(i_1,j_1)}^{(p,q)}(\xi,\eta) J|_{\hat{\Gamma}}(\xi,\eta) \, d\hat{\Gamma}.
\end{align*}

Denotaremos $\mathbf{M}=J^TJ$, de manera que $\mathbf{M}^{-1}=(J^TJ)^{-1}=J^{-1}J^{-T}$.

\begin{align*}
    &L(\phi_{(i_1,j_1)}(\mathbf{x})) = \int_{\Omega}f(\mathbf{x})\phi_{(i_1,j_1)}(\mathbf{x}) \, d\Omega \nonumber \\
    &\quad + \int_{\Gamma_N}h(\mathbf{x})\phi_{(i_1,j_1)}(\mathbf{x}) \, d\Gamma + \int_{\Gamma_R}\frac{r}{\beta}\phi_{(i_1,j_1)}(\mathbf{x}) \, d\Gamma =\\[10pt]
    &= \int_{v_0}^{v_{m_v}}\int_{u_0}^{u_{m_u}}f(\mathbf{G}(\xi,\eta))\hat{R}_{(i_1,j_1)}^{(p,q)}(\xi,\eta)|\det \mathbf{J}(\xi,\eta)| \, d\xi d\eta \nonumber \\
    &\quad + \int_{\hat{\Gamma}_N} h(\mathbf{G}(\xi,\eta))\hat{R}_{(i_1,j_1)}^{(p,q)}(\xi,\eta) J|_{\hat{\Gamma}}(\xi,\eta) \, d\hat{\Gamma} + \int_{\hat{\Gamma}_R} \frac{r}{\beta}\hat{R}_{(i_1,j_1)}^{(p,q)}(\xi,\eta) J|_{\hat{\Gamma}}(\xi,\eta) \, d\hat{\Gamma}.
\end{align*}

\subsection{Cálculo de las integrales}

Tomaremos ahora intervalos no nulos de la forma $\hat{\Omega}_e=[u_{n},u_{{n}+1})\times[v_{k}, v_{{k}+1})$.

Para este caso la \textbf{matriz local} $\mathbf{K}^e$ será de tamaño $(p+1)(q+1)\times(p+1)(q+1)$, con el fin de facilitar la comprensión de la correlación entre funciones base y elementos de la matriz tomaremos una apliación que relacione los indices de los elementos $\mathbf{K}^e_{(i,j)}$ con el número de funciones activas en cada dirección del elemento correspondiente:

\begin{align}
    \label{eq:indices}
    &i=(i_1-1)\cdot(q+1)+j_1=(i_1,j_1), \\
    &j=(i_2-1)\cdot(q+1)+j_2=(i_2,j_2), \quad i_1, i_2 \in \{1,2,\dots,p+1\}, \quad j_1, j_2 \in \{1,2,\dots,q+1\}.
\end{align}

Teniendo así la integral que le corresponde al elemento:

\begin{equation*} 
    \mathbf{K}^e_{((i_1,j_1),(i_2,j_2))} =\int_{v_k}^{v_{(k+1)}}\int_{u_n}^{u_{(n+1)}} \left(\hat{\nabla}\hat{R}_{(i_2 + n - p - 1, , j_2 + k - q - 1)}^{(p,q)}\right)^T \left( \mathbf{J}^{-1} \mathbf{J}^{-T} \right) \left(\hat{\nabla}\hat{R}_{(i_1 + n - p - 1, , j_1 + k - q - 1)}^{(p,q)}\right) |\det \mathbf{J}| \, d\xi d\eta.
\end{equation*}

Definimos de la misma manera ahora la \textbf{matriz local extendida} $\tilde{\mathbf{K}}^e$,

\begin{equation*}
    \tilde{\mathbf{K}}^e_{((i_1+n-p-1,j_1+k-q-1),(i_2+n-p-1, j_2+k-q-1))}=\mathbf{K}^e_{((i_1,j_1)(i_2,j_2))}.
\end{equation*}

Formando así la \textbf{matriz de rigidez} $\mathbf{K}$ mediante la suma de estas matrices:

\begin{equation*}
    \mathbf{K} = \sum_{e=1}^{N_{el}} \tilde{\mathbf{K}}^e.
\end{equation*}

Realicamos el mismo procedimiento para el vector de carga:

\begin{align*}
    &\mathbf{F}^e_{(i_1,j_1)}=\int_{v_k}^{v_{(k+1)}}\int_{u_n}^{u_{(n+1)}}f(\mathbf{G})\hat{R}_{(i_1,j_1)}^{(p,q)}|\det \mathbf{J}| \, d\xi d\eta. \\
    &\tilde{\mathbf{F}}^e_{(i_1+n-p-1,j_1+k-q-1)}=\mathbf{F}^e_{(i_1,j_1)}.\\
    &\mathbf{F}= \sum_{e=1}^{N_{el}} \tilde{\mathbf{F}}^e.
\end{align*}

\subsection{Imposición de condiciones de contorno}
Se aplicarán los siguientes algoritmos para los elementos que contengan a la forntera de nuestro espacio físico.

Sea $\hat{\Omega}_e=[u_{n},u_{{n}+1})\times[v_{k}, v_{{k}+1})$, un elemento paramétrico tal que $\mathbf{G}(\hat{\Omega_e})$ contiene parte de la frontera de $\Omega$, 
dado que la frontera de $\Omega$ coincide con la frontera paramétrica, suponemos, sin pérdida de generalidad, que la frontera correspondiente a $\mathbf{G}(\hat{\Omega_e})$ coincide únicamente 
con la frontera al borde paramétrico inferior, es decir, $\xi \in [u_{n},u_{{n}+1})$ y $\eta=v_k$\footnote{Para los elementos esquina basta aplicar el algoritmo dos veces, una para cada borde.}.

Recorreremos las funciones base asociadas a esta frontera, (en este caso recorremos el indice $i_1$ y fijamos $j_1=1$), para ello proyectamos de vuelta nuestros indices $I=(i_1,1)$ para $i_2=1,\dots,p+1$, mediante la ecuación \ref{eq:indices}.

\subsubsection{Tipo Dirichlet}
\begin{enumerate}
    \item Para cada índice $I$:
    \begin{itemize}
        \item Sustituimos la fila $I$-ésima de la matriz global $\mathbf{K}$ por ceros, imponiendo 1 en la diagonal ($\mathbf{K}_{I,I} = 1$).
        \item Actualizamos el vector de carga: $\mathbf{F}_I = g(\mathbf{x}_I)$.
    \end{itemize}
    \item Para mantener la simetría del sistema, eliminamos las columnas $I$ correspondientes:
    \begin{itemize}
        \item Para cada fila distinta de $I_2 \neq I$ donde $\mathbf{K}_{I_2,I} \neq 0$, modificamos el vector de carga restando la contribución conocida: $\mathbf{F}_K \leftarrow \mathbf{F}_K - \mathbf{K}_{I_2,I} \cdot g(\mathbf{x}_I)$.
        \item Establecemos $\mathbf{K}_{I_2,I} = 0$.
    \end{itemize}
\end{enumerate}


\subsubsection{Tipo Neumann}
Para cada índice $I$:
\begin{equation*}
        \mathbf{F}_I \leftarrow \mathbf{F}_I + \int_{u_n}^{u_{n+1}} h(\mathbf{G}(\xi, v_k)) \cdot \hat{R}_{(i_1,1)}^{(p,q)}(\xi, v_k) \cdot J_{\Gamma}(\xi) \, d\xi.
\end{equation*}

\subsubsection{Tipo Robin o Mixtas}

Proyectamos también en este caso $J = (i_2, 1)$ para $i_2=1,\dots,p+1$.

Para cada índice $I$:
    \begin{equation*}
        \mathbf{F}_I \leftarrow \mathbf{F}_I + \int_{u_n}^{u_{n+1}} \frac{r}{\beta} \cdot \hat{R}_{(i_1,1)}^{(p,q)}(\xi, v_k) \cdot J_{\Gamma}(\xi) \, d\xi.
    \end{equation*}

Para cada par de índices $(I, J)$:
    \begin{equation*}
        \mathbf{K}_{I,J} \leftarrow \mathbf{K}_{I,J} + \int_{u_n}^{u_{n+1}} \frac{\alpha}{\beta} \cdot \hat{R}_{(i_1,1)}^{(p,q)}(\xi, v_k) \cdot \hat{R}_{(i_2,1)}^{(p,q)}(\xi, v_k) \cdot J_{\Gamma}(\xi) \, d\xi.
    \end{equation*}





\section{Convergencia en L2 y H1 y Tiempo de Computación Teórico}

\subsection{Convergencia}

Para asegurar la convergencia debemos partir de unas condiciones iniciales:
\begin{itemize}
    \item \textbf{Regularidad de la solución:} $u\in H^{p+1}(\Omega)$.
    \item \textbf{Malla no degenerada:} det($J$)$>0$.
\end{itemize}

Bajo estas condiciones podemos tomar cotas del error en diferentes espacios:

\begin{itemize}
    \item \textbf{Norma en $H^1(\Omega)$:} $\| u - u_h \|_{H^1(\Omega)} \leq C_1 h^p | u |_{H^{p+1}(\Omega)}$.
    \item \textbf{Norma en $L^2(\Omega)$:} $\| u - u_h \|_{L^2(\Omega)} \le C_2 h^{p+1} | u |_{H^{p+1}(\Omega)}$.
\end{itemize}

\begin{proof}
    Las demostraciones están omitidas temporalmente.
\end{proof}

\subsection{Tiempo de Computación}

Separaremos el tiempo de computación en sus fases independientes:

\subsubsection{Fase de Evaluación}

Fase previa al ensamblado para dimensiones mayores o iguales que dos, que aprovecha las propiedades del producto tensorial para evitar recalcular evaluaciones de funciones base, su coste computacional:

$$\sum_{i=1}^{d}Q_i\cdot n_i=\mathcal{O}(d\cdot\text{max}_i\{Q_i\cdot n_i\}),$$ donde $Q_i$ es el número de evaluaciones direccionales y $n_i$ el número de divisiones correspondientes a la dimensión $i$.

\subsubsection{Fase de Ensamblado}

Fase en la que se realizan las integrales numéricas y se ensamblan, su coste computacional:

\begin{equation*}
    \prod_{i=1}^d Q_i^2 \cdot n_i \cdot C, 
\end{equation*}

Donde $C$ es el coste de la integración numérica, calculo del jacobiano y manipulación de la memoria.

\subsubsection{Fase de Resolución}

Depende del tamaño final del sistema y del método elegido para resolver el sistema resultante.

%--- FIN DEL CAPÍTULO 3

\clearemptydoublepage

%--- CAPÍTULO 4 DEL TFG
\chapter{Implementación computacional.}

Resumen breve de la intención del capitulo en ilustrar ejemplos con código funcional que se ha utilizado para obtener resultados numéricos.

Así como el uso de diferentes estructuras como matrices sparse o tensores y su necesidad para el correcto funcionamiento del código.

\section{Lenguaje y librerías utilizadas.}

Se mencionará C++ como lenguaje principal usando la librería Eigen para el uso de matrices sparse, así como clases y funciones que se añadirán en el anexo para tema NURBS. también habrá mención a Octave para visualizar los resultados.

\section{Metodos numéricos empleados.}

Explicación de integración numérica para las integrales que aparecen en las matrices de ensamblado y vector de cargo mediante la cuadratura de Gauss-Legendre, el metodo LU para resolver el sistema 1D y el método del gradiente conjugada con precondicionador de Choleski incompleto para resolver el sistema de 2D.

\section{Estructura del programa.}

Se explicaran con detalle y en orden como es la ejecución del programa y que funciones/operaciones se ejecutan en cada paso.

Creación de la estructura mediante NURBS.

Refinamiento del mallado.

Ensamblado elemento a elemento (dependiendo de cuanto tome este apartado se fragmentará).

Imposición de las condiciones de frontera.

Resolver el sistema final.

Construcción y evaluación de la solución a partir de las soluciones del sistema.

Cálculo de la norma L2 y H1.

Representación Gráfica en Octave.

(Con el fin de no superar el máximo de hojas permitidas, el codigo se mostrará en el anexo y no en este apartado)


%--- FIN DEL CAPÍTULO 4

\clearemptydoublepage

%--- CAPÍTULO 5 DEL TFG
\chapter{Resultados.}

En este capítulo se mostrarán tanto los resultados de convergencia como las gráficas de las soluciones, la frecuencia de estas respecto al refinamiento dependerá de cuantas páginas hayan sido escritas en total.
En ambas secciones se verificará que el codigo se ha ejecutado correctamente y su validación.

\section{Resultados en 1D}

\section{Resultados en 2D}

%--- FIN DEL CAPÍTULO 5

\clearemptydoublepage

%--- CAPÍTULO 6 DEL TFG
\chapter{Conclusiones.}

%-------------
\backmatter                      % Parte final del texto

%------ Bibliografía
% 1. Configuración para el Título en el Índice (TOC)
\phantomsection
%\chapter*{\bibname} 
\addcontentsline{toc}{chapter}{\bibname}
\chaptermark{\bibname}
\sectionmarkwithoutsections{\bibname}

% 2. Proceso de BibTeX
% Usar apacite para formato APA real que sí lee DOIs y URLs
\bibliographystyle{unsrtnat} % Define el estilo (APA-like)
\nocite{*}
\bibliography{Biblio2}
%--- FIN DE LA BIBLIOGRAFÍA

\chapter*{Anexos}
\addcontentsline{toc}{chapter}{Anexos}

\section{Método de los elementos finitos.}
\label{FEM}
Partiendo de la formulación variacional, con la garantía de la existencia y unicidad de la solución $u\in V(\Omega) \subset H^1(\Omega)$, en el FEM buscamos una solución aproximada $u^h$ de esta en un espacio de dimensión finita.

Para ello se realizan dos pasos previos:

\begin{itemize}
    \item \textbf{Discretización del dominio ($\Omega$):} este se divide en una colección de subdominios principales denominados \textbf{elementos}, estos elementos son politopos cerrados y simples (en 2D normalmente son triángulos, pero pueden ser también otro tipo de poligonos). Los denotaremos como $K_j$, de manera que se cumple que $\Omega \cup \Gamma = \overline{\Omega} = \bigcup_{j=1}^{M} K_j $. Esta subdivisión se conoce como malla ($T_h=\{K_1, K_2,\dots,K_M\})$.
    \item \textbf{Discretización del espacio ($V(\Omega$)):} el subespacio de dimensión finita construido a base de funciones polinómicas $V^h = \{ v \in H^1(\Omega) \mid v = g \text{ en } \Gamma_D \text{ y } v|_{K_j} \in P_p(K_j), \forall K_j \in T_h \}$, donde $p\in \mathbb{N}$ es el grado polinomial elegido y $h = \max_{K_j \in \mathcal{T}_h} h_j, \, h_j = \text{diam}(K_j) = \sup_{x, y \in K_j} \|x - y\|$, de manera que podemos definir una base de este de la forma $\{\phi_1, \phi_2, \dots, \phi_N\}$.
    
        Definimos adicionalmente los nodos $x_i \in \overline{\Omega}$ y el conjunto nodal $\mathcal{N} = \{ x_1, x_2, \dots, x_N \} \subset \overline{\Omega}$, donde N es la dimensión de $V^h$. El lugar que ocupa cada $x_i$ depende tanto del tipo de elementos utilizados para crear la malla como del grado polinomial $p$ elegido, además, cada uno de estos está asociado a una única función base, de manera que se cumplen las siguientes propiedades:
        \begin{itemize}
            \item $\phi_i(x_j) = \delta_{ij} = \begin{cases} 1 & \text{si } i = j \\ 0 & \text{si } i \neq j \end{cases}$
            \item $\text{supp}(\phi_i) = \overline{\bigcup \{K_j \in T_h \mid x_i \in K_j\}}$
        \end{itemize}
\end{itemize}

Tras esto definimos la solución aproximada y su gradiente como como:

\begin{equation*}
    u^h(x) = \sum_{i=1}^{N} U_i \phi_i(x), \quad U_i \in \mathbb{R}, \quad  \forall i=1,2, \dots , N.
\end{equation*}
\begin{equation*}
    \mathbf{\nabla u^h(x) = \nabla \left( \sum_{i=1}^{N} U_i \phi_i(x) \right) = \sum_{i=1}^{N} U_i \nabla \phi_i(x)}.
\end{equation*}

Luego el problema se resume en encontrar $u\in v^h$ tal que $a(u^h, v^h)-L(v^h)=0,\, \forall v^h\in V^h$. sustituyendo y aplicando la 
\textbf{linealidad de la forma bilineal\footnote{$a(\sum_{j=1}^{N} U_j \phi_j,\sum_{i=1}^{N} U_i \phi_i)=\sum_{i=1}^{N}V_i\sum_{j=1}^{N}U_i a(\phi_j,\phi_i).$}
 y de la forma lineal\footnote{$L(\sum_{i=1}^{N}V_i \phi_i)=\sum_{i=1}^{N}V_i L(\phi_i)$.}}
 nos queda:

\begin{equation*}
    \sum_{i=1}^{N} V_i \left[ \sum_{j=1}^{N} a(\phi_j, \phi_i) U_j - L(\phi_i) \right] = 0, \quad \forall V_i \in \mathbb{R}, \quad \forall i=1,2,\dots , N.
\end{equation*}

Finalizando por tanto con el sistema dado por $\sum_{j=1}^{N} a(\phi_j, \phi_i) U_j = L(\phi_i),\quad \forall i=1,2,\dots , N.$

\begin{defin} Los principales elementos del sistema que se resolverá para obtener la solución son:
\begin{itemize}
    \item \textbf{La matriz de rigidez A: } $A_{ij}=a(\phi_j,\phi_i)=\int_{\Omega}\nabla \phi_j \cdot \nabla \phi_i \, d\Omega + \int_{\Gamma_R}\frac{\alpha}{\beta}\phi_j \phi_i \, d\Gamma.$
    \item \textbf{El vector de carga F: } $F_i=L(\phi_i)=\int_{\Omega}f \phi_i \, d\Omega +\int_{\Gamma_N}h\phi_i \, d\Gamma+\int_{\Gamma_R}\frac{r}{\beta}\phi_i \,d\Gamma.$
\end{itemize}
\end{defin}\footnote{Si se desea conocer más sobre algoritmos de ensamblado de $\mathbf{A}$ y $\mathbf{F}$, consúltese \cite{larson_finite_2013}, especialmente los capítulos 4 y 5.}
Finalizando con el sistema $AU=F$, donde $U=( U_1, U_2, \dots , U_N )^T$, adicionalmente $u^h(x_i)=U_i$.

\begin{defin}
Definimos ahora la norma inducida por la forma bilineal $a( \cdot , \cdot)$, que denotaremos por $\| \cdot \|_V$:
\begin{equation*}
    \|v\|_V^2=a(v,v)=\int_{\Omega} \nabla u \cdot \nabla v \, d\Omega + \int_{\Gamma_R} \frac{\alpha}{\beta} u v \, d\Gamma.
\end{equation*}
\end{defin}

\begin{lemma}
    Las normas $\|\cdot\|_V$ y $\|\cdot\|_{H^1(\Omega)}$ son equivalentes, es decir, existen $C_1, \, C_2 \in \mathbb{R}$ tal que:

\begin{equation*}
    C_1 \|v\|_{H^1(\Omega)}^2 \leq \|v\|_{V}^2 \leq C_2 \|v\|_{H^1(\Omega)}^2.
\end{equation*}

\begin{proof}[Demostración omitida]
    La demostración puede consultarse en el Anexo \ref{sec:normas}.
    \renewcommand{\qedsymbol}{}
\end{proof}
\end{lemma}



    
\begin{teorema} El error $e=u-u^h$ del FEM converge en las normas $\|\cdot\|_{L^2(\Omega)}$ y $\|\cdot\|_{H^1(\Omega)}$, tal que:
    \begin{itemize}
        \item $\|e\|_{H^1(\Omega)}$, se reduce con orden  $\mathcal{O}(h^{p})$.
        \item $\|e\|_{L^2(\Omega)}$, se reduce con orden  $\mathcal{O}(h^{p+1})$.
    \end{itemize}
    Siempre que $u\in H^{p+1}(\Omega)$.

\begin{proof}
    Separaremos la demostración en cada una de las normas:
\begin{itemize}
    \item \textbf{Convergencia en } $\|\cdot\|_{H^1(\Omega)}$:

    Asumiendo la coercividad y continuidad de la forma bilineal en $V(\Omega)$\footnote{La demostración de estas puede ser encontrada en el anexo \ref{sec:Cea}}, nos encontramos en las condiciones del \textbf{Lema de Céa} por tanto se cumple:
    \begin{equation*}
        \|u - u_h\|_V \leq \frac{C_{cont}}{C_{coer}} \inf_{v_h \in V_h} \|u - v_h\|_V, 
    \end{equation*}
    donde $C_{cont}$ es la constante de continuidad y $C_{coer}$ es la constante de coercividad.

    Aplicando ahora la equivalencia entre normas $\| \cdot \|_V$ y $\| \cdot \|_{H^1(\Omega)}$ obtenemos que $\exists \, C_{\text{Céa}} \in \R^+$ tal que:
    \begin{equation}
        \|u - u_h\|_{H^1(\Omega)} \leq C_{\text{Céa}} \inf_{v_h \in V_h} \|u - v_h\|_{H^1(\Omega)}.
    \label{eq:cea}
    \end{equation}

    Por último usaremos la \textbf{propiedad de aproximación del espacio de Sobolev}, la cual nos garantiza que $\exists \, C_{\text{Sob}} \in \R^+$ tal que:
    \begin{equation}
        \inf_{v_h \in V_h} \|u - v_h\|_{H^1(\Omega)} \leq C_{\text{Sob}} h^{p} \|u\|_{H^{p+1}(\Omega)}.
    \label{eq:sob}
    \end{equation}
    Finalmente uniendo todo y definiendo $C_{\text{Cota}}=C_{\text{Céa}}C_{\text{Sob}}\|u\|_{H^{p+1}(\Omega)}$ tenemos:
    \begin{equation*}
        \|u - u_h\|_{H^1(\Omega)} \leq C_{\text{Cota}} h^{p},
    \end{equation*} 
    obteniendo así un error en $H^1(\Omega)$ que converge con orden $\mathcal{O}(h^{p})$, siempre que $u\in H^{p+1}(\Omega)$.

    \item \textbf{Convergencia en } $\|\cdot\|_{L^2(\Omega)}$:
    
    Para la cota del error $e=u-u_h$ en $L^2(\Omega)$ consideramos el problema dual asociado:

    Encontrar $w\in V(\Omega)$ tal que:
    \begin{equation*}
        a(v,w) = (e,v)_{L^2(\Omega)}=\int_\Omega ev \, d\Omega, \quad \forall v \in V(\Omega).
    \end{equation*}

    Debido a la coercividad y continuidad de la forma bilineal y la continuidad de la forma lineal\footnote{La demostración de la continuidad de la forma lineal se encuentra en el anexo \ref{sec:cont}}, debido al \textbf{Lema de Lax-Millgram}, la solución w existe y es única, su regularidad viene dada por las condiciones de regularidad elípticas asumidas en el anexo \ref{sec:equivform}, luego $\exists \, C_{\text{Reg}}\in \R^+$ tal que:
    \begin{equation*}
        \|w\|_{H^2(\Omega)} \leq C_{\text{reg}} \|e\|_{L^2(\Omega)}
    \end{equation*}

    Tomamos ahora $v=e$, obteniendo así la identidad
    \begin{equation*}
        a(e,w) = (e,e)_{L^2(\Omega)}=\|e\|^2_{L^2(\Omega)}.
    \end{equation*}

    Como la solución discreta $u_h$ satisface la \textbf{propiedad de ortogonalidad de Galerkin}:
    \begin{equation*}
        a(u-u_h, v_h) = a(e, v_h) = 0, \quad \forall v_h \in V_h.
    \end{equation*} 

    Nos permite introducir cualquier función interpolante $w_h \in V_h$ tal que se cumple la siguiente igualdad:
    \begin{equation*}
        \|e\|_{L^2(\Omega)}^2 = a(e, w) - a(e, w_h) = a(e, w - w_h).
    \end{equation*}

    Aplicando ahora la \textbf{continuidad de la forma bilineal},
    \begin{equation*}
        \|e\|_{L^2(\Omega)}^2 \leq C_{\text{cont}} \|e\|_{H^1(\Omega)} \|w - w_h\|_{H^1(\Omega)}.
    \end{equation*}

    Usamos ahora el resultado de convergencia para $\|e\|_{H^1(\Omega)}$ dadas por \ref{eq:cea} y \ref{eq:sob}
    \begin{equation*}
        \|e\|_{L^2(\Omega)}^2 \leq C_{\text{cont}}C_{\text{Céa}}C_{Sob}h^p\|u\|_{H^{p+1}(\Omega)}\|w - w_h\|_{H^1(\Omega)}.
    \end{equation*}
    
    Terminamos utilizando la \textbf{propiedad de aproximación de los espacios de Sobolev}, y tomando que $w\in H^2(\Omega)$ debido a la regularidad elíptica tenemos:
    \begin{equation*}
        \|w - w_h\|_{H^1(\Omega)} \leq C_{2} h^{2-1} \|w\|_{H^{2}(\Omega)} = C_{2} h^{1} \|w\|_{H^{2}(\Omega)}.
    \end{equation*}

    Culminando en la desigualdad:
    \begin{equation*}
        \|e\|_{L^2(\Omega)}^2 \leq \left( C_{\text{cont}}C_{\text{Céa}}C_{Sob}C_{2} \|u\|_{H^{p+1}(\Omega)} \|w\|_{H^{2}(\Omega)} \right)h^{p+1}.
    \end{equation*}
    obteniendo así un error en $L^2(\Omega)$ que converge con orden $\mathcal{O}(h^{p+1})$, siempre que $u\in H^{p+1}(\Omega)$.
\end{itemize}
\end{proof}
\end{teorema}


\section{Demmostraciones omitidas}
\subsection{Paso de solución fuerte a solución variacional}
\label{sec:Paso_Soluciones}
\begin{proof}
    

Recurriremos al \textbf{Principio de Galerkin}, el cual nos garantiza que:
\begin{equation*}
    \int_{\Omega} (-\Delta u - f) v \, d\Omega = 0 \quad \text{para todo } v \in V_0(\Omega).
\end{equation*}

Reordenando y utilizando la identidad $\Delta u \cdot v = \nabla \cdot (\nabla u \cdot v) - \nabla u \cdot \nabla v$:

\begin{equation}
\int_{\Omega} (-\Delta u) v \, d\Omega = \int_{\Omega} -[\nabla \cdot (\nabla u \cdot v) - \nabla u \cdot \nabla v] \, d\Omega =  \int_{\Omega} \nabla u \cdot \nabla v \, d\Omega - \int_{\Omega} \nabla \cdot (\nabla u \cdot v) \, d\Omega.
\end{equation}


Al aplicar el \textbf{Teorema de Gauss-Ostrogradsky} al último término:
\begin{equation*}
    \int_{\Omega} \nabla \cdot (\nabla u \cdot v) \, d\Omega = \int_{\Gamma} (\nabla u \cdot \mathbf{n}) v \, d\Gamma.
\end{equation*}
Sustituyendo esta conversión en la ecuación $\eqref{eq:6.1}$ obtenemos la \textbf{Formulación variacional preliminar}:
\begin{equation*}
\int_{\Omega} \nabla u \cdot \nabla v \, d\Omega - \int_{\Gamma} (\nabla u \cdot \mathbf{n}) v \, d\Gamma = \int_{\Omega} f v \, d\Omega.
%\label{eq:2.2}
\end{equation*}

Separando ahora $\Gamma$ en sus componentes disjuntas:

\begin{equation*}
\int_{\Gamma} (\nabla u \cdot \mathbf{n}) v \, d\Gamma = \int_{\Gamma_D} (\nabla u \cdot \mathbf{n}) v \, d\Gamma + \int_{\Gamma_N} (\nabla u \cdot \mathbf{n}) v \, d\Gamma + \int_{\Gamma_R} (\nabla u \cdot \mathbf{n}) v \, d\Gamma.
\end{equation*}

Utilizamos ahora $v \in V_0(\Omega)$ y las condiciones de contorno:

\begin{equation*}
\int_{\Gamma} (\nabla u \cdot \mathbf{n}) v \, d\Gamma = 0 + \int_{\Gamma_N} h v \, d\Gamma + \int_{\Gamma_R} (\frac{r-\alpha u}{\beta}) v \, d\Gamma.
\end{equation*}

Por último sustituimos y reordenamos en forma bilineal $a(u,v)$ igualada a la forma lineal $L(v)$, obteniendo así:

\begin{align*}
     &{a(u,v)}=\int_{\Omega}\nabla u \cdot \nabla v\, d\Omega + \int_{\Gamma_R}\frac{\alpha}{\beta}uv\, d\Gamma=\\
     &=\int_{\Omega}fv \, d\Omega+\int_{\Gamma_N}hv\, d\Gamma+\int_{\Gamma_R}\frac{r}{\beta}v\, d\Gamma={L(v)}.
\end{align*}
\end{proof}
\subsection{Existencia y unicidad de la solución variacional (Lema de Lax-Millgram)}
\label{sec:Lax}
\begin{teorema}
La existencia y unicidad de la solución al problema variacional $u \in V(\Omega)$ está garantizada gracias al \textbf{Lema de Lax-Milgram}, pues se cumplen las tres siguientes condiciones de este:

\begin{itemize}
    \item \textbf{Continuidad de la Forma Bilineal: }$ | a(u,v) | \leq (1+C^2_{Tr}\left|\frac{\alpha}{\beta}\right|) \|u \|_{H^1(\Omega)}\|  v \|_{H^1(\Omega)}.$
    \item \textbf{Coercividad de la Forma Bilineal: }$ a(v,v) \geq  \| \nabla v \|^2_{L^2(\Omega)} \geq \frac{1}{C_P+1}\|v\|^2_{H^1(\Omega)}. $
    \item \textbf{Continuidad de la Forma Lineal: } $ |L(v) | \leq C\|v\|_{H^1(\Omega)}, \quad C\in \R^+. $
    
\end{itemize}
\end{teorema}

\begin{itemize}
    \item \textbf{Continuidad de la Forma Bilineal:}\begin{proof} aplicaremos la \textbf{desigualdad triangular},
    \[ | a(u,v) | = \left| \int_{\Omega}\nabla u \cdot \nabla v\, d\Omega + \int_{\Gamma_R}\frac{\alpha}{\beta}uv\, d\Gamma \right| \leq \left| \int_{\Omega}\nabla u \cdot \nabla v\, d\Omega \right| + \left| \int_{\Gamma_R}\frac{\alpha}{\beta}uv\, d\Gamma \right|. \]
    Continuaremos con la \textbf{desigualdad de Cauchy-Schwartz} para el primer término,
    \[ \left| \int_{\Omega}\nabla u \cdot \nabla v\, d\Omega \right| \leq  \int_{\Omega} \left|\nabla u \right| \left|\nabla v \right|\, d\Omega \leq \| \nabla u \|_{L^2(\Omega)}\| \nabla v \|_{L^2(\Omega)} \leq \|  u \|_{H^1(\Omega)}\|  v \|_{H^1(\Omega)} .\]
    Buscamos ahora acotar el segundo término utilizando primero la \textbf{desigualdad de Cauchy-Schwartz}:
    \[ \left| \int_{\Gamma_R}\frac{\alpha}{\beta}uv\, d\Gamma \right| \leq  \left|\frac{\alpha}{\beta}\right|\int_{\Gamma_R}|u||v|\, d\Gamma \leq \left|\frac{\alpha}{\beta}\right| \|  u \|_{L^2(\Gamma_R)}\|  v \|_{L^2(\Gamma_R)} .\]
    Usamos ahora el \textbf{teorema de la traza} que nos garantiza la existencia de una constante $C_{Tr}$ tal que,
    \[ \left|\frac{\alpha}{\beta}\right| \|  u \|_{L^2(\Gamma_R)}\|  v \|_{L^2(\Gamma_R)} \leq C^2_{Tr} \left|\frac{\alpha}{\beta}\right| \|  u \|_{H^1(\Omega)}\|  v \|_{H^1(\Omega)}.\]
    Por lo que obtenemos:
    \[ | a(u,v) | \leq (1+C^2_{Tr}\left|\frac{\alpha}{\beta}\right|) \|u \|_{H^1(\Omega)}\|  v \|_{H^1(\Omega)}.\]
    \end{proof}
    \item \textbf{Coercividad de la Forma Bilineal:}\begin{proof} partimos de la condición impuesta $\frac{\alpha}{\beta} \geq 0$:
    \[ a(v,v) = \int_\Omega \nabla v \cdot \nabla v \, d\Omega + \int_{\Gamma_R} \frac{\alpha}{\beta}v^2 \, d\Gamma = \int_\Omega |\nabla v|^2 \, d\Omega + \int_{\Gamma_R} \frac{\alpha}{\beta} v^2 \, d\Gamma \geq \int_\Omega |\nabla v|^2 \, d\Omega = \| \nabla v \|^2_{L^2(\Omega)}. \]
    Partiendo de que $\Omega$ es acotado y $v \in V_0(\Omega)$ aplicamos la \textbf{desigualdad de Poincaré} la cual nos dice:
    \[\text{Existe $C_P > 0$ tal que: } \|v\|^2_{L^2(\Omega)} \leq C_P\|\nabla v\|^2_{L^2(\Omega)}. \]
    Recordamos ahora la definición de la norma H1 y aplicamos la \textbf{desigualdad de Poincaré}:
    \[ \|v\|^2_{H^1(\Omega)}=  \|v\|^2_{L^2(\Omega)} +  \|\nabla v\|^2_{L^2(\Omega)} \leq (C_P+1)\|\nabla v\|^2_{L^2(\Omega)} .\]
    Dividiendo ambos lados por $(C_P+1)$  y sustituyendo en la desigualdad de la forma bilinear anterior obtenemos:
    \[ a(v,v) \geq  \| \nabla v \|^2_{L^2(\Omega)} \geq \frac{1}{C_P+1}\|v\|^2_{H^1(\Omega)}. \]
    \end{proof}
    \item \textbf{Continuidad de la Forma Lineal:}\begin{proof} partimos de definición de $L(v)$ y aplicamos la \textbf{desigualdad triangular},
    \[ |L(v) | \leq \left| \int_\Omega fv \,d\Omega \right| +  \left| \int_{\Gamma_N}hv \, d\Gamma \right| + \left| \int_{\Gamma_R}\frac{r}{\beta}v \, d\Gamma \right|.\] 
    Acotaremos ahora término a término,
    \[ \left| \int_\Omega fv \,d\Omega \right| \leq \|f\|_{L^2(\Omega)}\|v\|_{L^2(\Omega)}\leq \|f\|_{L^2(\Omega)}\|v\|_{H^1(\Omega)} .\]
    \[ \left| \int_{\Gamma_N} hv \,d\Gamma \right| \leq \|h\|_{L^2(\Gamma_N)}\|v\|_{L^2(\Gamma_N)} \leq \|h\|_{L^2(\Gamma_N)}C_{Tr}\|v\|_{H^1(\Omega)}, \textbf{(Teorema de la traza)}.\]
    \[ \left| \int_{\Gamma_R}\frac{r}{\beta}v \, d\Gamma \right| \leq \left|\frac{1}{\beta} \right| \|r\|_{L^2(\Gamma_R)}\|v\|_{L^2(\Gamma_R)} \leq  \left|\frac{1}{\beta} \right| \|r\|_{L^2(\Gamma_R)}C_{Tr}\|v\|_{H^1(\Omega)}, \textbf{(Teorema de la traza)} .\]
    Finalmente, la continuidad se demuestra al acotar:
    \[ |L(v) | \leq \left(  \|f\|_{L^2(\Omega)}\| +  \|h\|_{L^2(\Gamma_N)}C_{Tr} + \left|\frac{1}{\beta} \right|\|r\|_{L^2(\Gamma_R)}C_{Tr}   \right)\|v\|_{H^1(\Omega)}. \]
    \end{proof}
\end{itemize}

El cumplimiento de estas condiciones para el operador Laplaciano asegura que el problema variacional está \textbf{bien planteado}.

\subsection{Equivalencia de la solución de la formulación variacional y fuerte}
\label{sec:equivform}
\begin{proof}
Asumimos $\Gamma \in C^2$, $f \in L^2(\Omega)$,  $g \in H^{3/2}(\Gamma_D)$, $h \in H^{1/2}(\Gamma_N)$, $r \in H^{1/2}(\Gamma_R)$ y $u\in H^2(\Omega)$, recordamos ahora la formulación variacional:

\begin{equation}
    \int_{\Omega}\nabla u \cdot \nabla v\, d\Omega + \int_{\Gamma_R}\frac{\alpha}{\beta}uv\, d\Gamma ={a(u,v)}={L(v)}=\int_{\Omega}fv \, d\Omega+\int_{\Gamma_N}hv\, d\Gamma+\int_{\Gamma_R}\frac{r}{\beta}v\, d\Gamma.
\label{eq:6.1}
\end{equation}

Como $u\in H^2(\Omega)$ podemos aplicar el teorema de divergencia inverso al primer término de la forma bilineal:
\begin{equation*}
     \int_{\Omega}\nabla u \cdot \nabla v\, d\Omega = -\int_\Omega (\Delta u)v \, d\Omega + \int_{\Gamma} (\nabla u \cdot n)v \, d\Gamma.
\end{equation*}
%\clearemptydoublepage
Sustituyendo ahora en $\eqref{eq:6.1}$ y agrupando todo en un lado de la igualdad obtenemos:

\begin{equation*}
    -\int_\Omega (\Delta u)v \, d\Omega + \int_{\Gamma} (\nabla u \cdot n)v \, d\Gamma + \int_{\Gamma_R}\frac{\alpha}{\beta}uv\, d\Gamma -\int_{\Omega}fv \, d\Omega-\int_{\Gamma_N}hv\, d\Gamma-\int_{\Gamma_R}\frac{r}{\beta}v\, d\Gamma = 0.
\end{equation*}

Separamos $\Gamma$ en sus componentes disjuntas $\Gamma_N$, $\Gamma_D$, $\Gamma_R$ y como $v\in V_0(\Omega)$, $v=0$ en $\Gamma_D$ luego:

\begin{equation*}
    \int_{\Gamma} (\nabla u \cdot \mathbf{n})v \, d\Gamma = \int_{\Gamma_N}  (\nabla u \cdot \mathbf{n})v \, d\Gamma + \int_{\Gamma_R}  (\nabla u \cdot \mathbf{n})v \, d\Gamma.
\end{equation*}

Reagrupamos ahora los términos que se integran en las mismas regiones:

\begin{equation*}
    \int_\Omega(-\Delta u- f)v \, d\Omega + \int_{\Gamma_N}(\nabla u \cdot \mathbf{n} -h)v \, d\Gamma +\int_{\Gamma_R} (\nabla u \cdot \mathbf{n} + \frac{\alpha}{\beta}u -\frac{r}{\beta})v \, d\Gamma = 0.
\end{equation*}

Aplicando ahora el \textbf{teorema fundamental del cálculo de variaciones} tenemos que todos los elementos entre paréntesis equivalen a 0 luego:

\begin{equation*}
\begin{cases}
- \Delta u = f & \text{en } \Omega, \\[6pt]
u = g & \text{en } \Gamma_D \quad (u\in V(\Omega)), \\[6pt]
\nabla u \cdot \mathbf{n} = h & \text{en } \Gamma_N, \\[6pt]
\alpha u + \beta \, \nabla u \cdot \mathbf{n} = r & \text{en } \Gamma_R.
\end{cases}
\end{equation*}
\end{proof}
\subsection{Equivalencia de normas}
\label{sec:normas}

\begin{proof}Demostraremos ambas cotas por separado:

    \begin{itemize}
        \item \textbf{Acotación superior} ($\|v\|_{V}^2 \leq C_2 \|v\|_{H^1(\Omega)}^2$), iniciamos recordando la definición de cada norma:
        
        \begin{itemize} 
            \item $\|v\|_{V}^2 = \int_{\Omega} |\nabla v|^2 \, d\Omega + \int_{\Gamma_R} \frac{\alpha}{\beta} |v|^2 \, d\Gamma$
            \item $\|v\|_{H^1(\Omega)}^2 = \int_{\Omega} |\nabla v|^2 \, d\Omega + \int_{\Omega} |v|^2 \, d\Omega$
        \end{itemize}

        Aplicaremos ahora la \textbf{desigualdad de la traza} obteniendo:
        \begin{equation*}
            \int_{\Gamma_R} |v|^2 \, d\Gamma \leq C_{Tr} \|v\|_{H^1(\Omega)}^2.
        \end{equation*}
        Usaremos también la desigualdad trivial:
        \begin{equation*}
            \int_{\Omega} |\nabla v|^2 \, d\Omega \leq \|v\|_{H^1(\Omega)}^2.
        \end{equation*}
        Obteniendo finalmente:
        \begin{equation*}
            \|v\|_{V}^2 \leq \|v\|_{H^1(\Omega)}^2+C_{Tr} \|v\|_{H^1(\Omega)}^2=(1+C_{Tr}) \|v\|_{H^1(\Omega)}^2.
        \end{equation*}
        Luego tomaremos $C_2$=$1+C_{Tr}$.
        \item \textbf{Acotación inferior} ($C_1 \|v\|_{H^1(\Omega)}^2 \leq \|v\|_{V}^2$), partimos de la \textbf{desigualdad de Poincaré generalizado}, el cual nos dice que existe $C_{P}\in \R^+$ tal que:
        \begin{equation*}
            \int_{\Omega} |v|^2 \, d\Omega \leq C_P \left( \int_{\Omega} |\nabla v|^2 \, d\Omega + \int_{\Gamma_R} |v|^2 \, d\Gamma \right)
        \end{equation*}
        Sustituyendo ahora en $\|v\|_{H^1(\Omega)}$:
        \begin{equation*}
            \|v\|_{H^1(\Omega)}^2 \leq \int_{\Omega} |\nabla v|^2 \, d\Omega + C_{P} \left( \int_{\Omega} |\nabla v|^2 \, d\Omega + \int_{\Gamma_R} |v|^2 \, d\Gamma \right) = (1 + C_P) \int_{\Omega} |\nabla v|^2 \, d\Omega + C_P \int_{\Gamma_R} |v|^2 \, d\Gamma.
        \end{equation*} 
        Buscamos ahora una constante $C_R$ tal que $C_R \cdot \|v\|^2_V\geq \|v\|^2_{H^1(\Omega)}$ y cumpla las siguientes propiedades:
        \begin{itemize}
            \item $C_{R} \geq (1 + C_P) \implies C_R \geq 1 + C_P.$
            \item $C_R\cdot \frac{\alpha}{\beta} \geq C_P \implies C_R \geq C_P \cdot \frac{\beta}{\alpha}$
        \end{itemize}
        Luego tomamos:
        \begin{equation*}
           C_R = max\left(1+C_P, \frac{\beta C_P}{\alpha}   \right).
        \end{equation*} 
        Obteniendo así la siguiente desigualdad:
        \begin{equation*}
           C_R \|v\|_{V}^2 = C_R \int_{\Omega} |\nabla v|^2 \, d\Omega + C_R \frac{\alpha}{\beta} \int_{\Gamma_R} |v|^2 \, d\Gamma \geq (1 + C_P) \int_{\Omega} |\nabla v|^2 \, d\Omega + C_P \int_{\Gamma_R} |v|^2 \, d\Gamma.
        \end{equation*}
        Tomando finalmente $C_1=\frac{1}{C_R}$:
        \begin{equation*}
           \|v\|_{V}^2\geq C_1\|v\|^2_{H^1(\Omega)}.
        \end{equation*}
    \end{itemize}
    

\end{proof}
\subsection{Condiciones del Lema de Céa}
\label{sec:Cea}
\begin{proof} Probaremos la coercividad y la continuidad en $\|\cdot\|_V$:
\begin{itemize}
    \item \textbf{Coercividad: } $\exists \, C_1\in \R^+ \text{ tal que }\, C_1 \|v\|_{H^1(\Omega)}^2 \leq \|v\|_{V}^2,$, Probado en la equivalencia de normas.
    \item \textbf{Continuidad: } $\exists \, C_2\in \R^+ \text{ tal que }\, |a(u, v)| \leq C_2 \|u\|_{H^1(\Omega)} \|v\|_{H^1(\Omega)}$:
     Iniciamos aplicando la \textbf{desigualdad triangular} a la definición de  $|a(u, v)|$:
     \begin{equation*}
        |a(u, v)| \leq \left| \int_{\Omega} \nabla u \cdot \nabla v \, d\Omega \right| + \left| \int_{\Gamma_R} \frac{\alpha}{\beta} u v \, d\Gamma \right|.
     \end{equation*}
     Aplicamos ahora la \textbf{desigualdad de Cauchy-Schwartz} en $L^2(\Omega)$ al primer término:
     \begin{equation*}
        \left| \int_{\Omega} \nabla u \cdot \nabla v \, d\Omega \right| \leq \int_{\Omega} |\nabla u| |\nabla v| \, d\Omega\leq \| \nabla u \|_{L^2(\Omega)} \| \nabla v \|_{L^2(\Omega)}.
     \end{equation*}
     Usamos ahora la propiedad trivial $\|v\|_H^1(\Omega) \geq \|\nabla v\|_L^2(\Omega), \forall v \in H^1(\Omega)$,
     \begin{equation*}
        |a(u, v)| \leq \| \nabla u \|_{L^2(\Omega)} \| \nabla v \|_{L^2(\Omega)} \leq \| u \|_{H^1(\Omega)} \| v \|_{H^1(\Omega)} .
     \end{equation*}
     Pasamos ahora al segundo término e empezamos aplicando la \textbf{desigualdad de Cauchy-Schwartz}:
     \begin{equation*}
        \left| \int_{\Gamma_R} \frac{\alpha}{\beta} u v \, d\Gamma \right| \leq \frac{\alpha}{\beta} \int_{\Gamma_R} |u| |v| \, d\Gamma \leq \frac{\alpha}{\beta} \|u\|_{L^2(\Gamma_R)} \|v\|_{L^2(\Gamma_R)}.
     \end{equation*}
     Seguiremos aplicando ahora la \textbf{desigualdad de la traza}:
     \begin{equation*}
        \|  u \|_{L^2(\Gamma_R)} \| v \|_{L^2(\Gamma_R)} \leq C_{Tr}^2 \|u\|_{H^1(\Omega)} \|v\|_{H^1(\Omega)}.
     \end{equation*}
     Luego uniendo ambas cotas de los términos individuales obtenemos:
     \begin{equation*}
        |a(u, v)| \leq \| u \|_{H^1(\Omega)} \| v \|_{H^1(\Omega)}  + \frac{\alpha}{\beta} C_{Tr}^2 \|u\|_{H^1(\Omega)} \|v\|_{H^1(\Omega)} = (1+\frac{\alpha}{\beta}C_{Tr}^2)\| u \|_{H^1(\Omega)} \| v \|_{H^1(\Omega)}.
     \end{equation*}
     Por último tomamos $C_2=1+\frac{\alpha}{\beta}C_{Tr}^2$.
\end{itemize}
\end{proof}

\subsection{Continuidad de la forma lineal dual}
\label{sec:cont}
\begin{proof}
    Recordamos que la forma lineal del problema dual es $L_\text{dual}(v)=(e,v)_{L^2(\Omega)}$.

    Iniciamos usando la \textbf{desigualdad de Cauchy-Schwartz} en $L^2(\Omega)$,
    \begin{equation*}
        |(e,v)_{L^2(\Omega)}| \leq \|e\|_{L^2(\Omega)}\|v\|_{L^2(\Omega)}.
    \end{equation*}
    
    Aplicamos ahora la \textbf{propiedad trivial} de la acotación superior  ${\|\cdot\|_{L^2(\Omega)}}$ por $\|\cdot\|_{H^1(\Omega)}$,
    \begin{equation*}
        |(e,v)_{L^2(\Omega)}| \leq \|e\|_{L^2(\Omega)}\|v\|_{H^1(\Omega)}.
    \end{equation*}
    Como $e=u-u^h$ es constante en este contexto, $e\in L^2(\Omega)$, luego tomamos $C_\text{L}=\|e\|_{L^2(\Omega)}$.
\end{proof}
\subsection{Propiedades de los B-Splines}
\label{sec:B-Splines_proof}
\begin{proof} Se demostrarán las propiedades en el orden en el que han sido establecidas:


    \begin{enumerate}
        \item \textbf{Soporte Local}: procederemos mediante inducción sobre $p$:
            \begin{itemize}
                \item \textbf{Caso Base ($p=0$)}: tenemos, $N_{i,0}(u)=1$ si $u\in [u_i, u_{i+1})$ por definición.
                \item \textbf{Paso de inducción sobre $p$}: partiendo de la definición mediante el algoritmo de Cox-de Boor:
                \begin{equation}
                    N_{i,p}(u) = \frac{u - u_i}{u_{i+p} - u_i} N_{i,p-1}(u) + \frac{u_{i+p+1} - u}{u_{i+p+1} - u_{i+1}} N_{i+1,p-1}(u).
                    \label{alg:Cox}
                \end{equation}
                Asumimos que $N_{i,p-1}(u)$ tiene soporte en $[u_i, u_{i+p})$ y $N_{i+1,p-1}(u)$ tiene soporte en $[u_{i+1}, u_{i+p+1})$, luego $N_{i,p}(u)$ tiene soporte $[u_{i}, u_{i+p+1})$.
            \end{itemize}
            \item \textbf{No Negatividad}: procederemos mediante inducción sobre $p$:
            \begin{itemize}
                \item \textbf{Caso Base ($p=0$)}: tenemos por definición que $N_{i,}=0$ o $1$, luego $N_{i,} \geq 0$.
                \item \textbf{Paso de inducción sobre $p$}: asumimos cierta la hipótesis para $p-1$, tomando $u \in [u_i, u_{i+p+1})$ y aplicando el algoritmo \ref{alg:Cox}, la demostración se reduce a probar que:
                \begin{itemize}
                    \item $\frac{u - u_i}{u_{i+p} - u_i}\geq 0$, trivial pues por como esta definido el vector de nodos, $u_{i+p} \geq u_i$ y como $u \in [u_i, u_{i+p+1})$, $u \geq u_i$
                    \item $\frac{u_{i+p+1} - u}{u_{i+p+1} - u_{i+1}}\geq 0$, de manera análoga tenemos $u_{i+p+1} \geq u_{i+1}$ y $u_{i+p+1}\geq u$.
                \end{itemize}
            
            \end{itemize}
            \item \textbf{Número de Bases Activas}: procederemos mediante inducción sobre $p$:
                \begin{itemize}
                    \item \textbf{Caso Base ($p=0$)}: por definición del caso $p=0$ de la funciones base y por la propiedad de no decrecimiento del vector de nudos, si $u\in [u_i, u_{i+1})$, existe unicamente una función base $N_{i,0}$ no nula en ese intervalo, luego el único indice $j$ que satisface la condición de tener una función base no nula asociada es $i=j$.
                    \item \textbf{Paso de inducción sobre $p$}: asumimos que para el intervalo $[u_i, u_{i+1})$ existen a lo sumo $p$ funciones base $N_{j,p-1}$ no nulas con indices $j\in \{i-(p-1),\dots , i\}$, debido al algoritmo de Cox-de Boor
                    \ref{alg:Cox} y a la no negatividad de los coeficientes y de las funciones base provada en el apartado anterior,si $u\in [u_i, u_{i+1})$, $N_{j,p}(u)$ será no nula en el intervalo $ [u_i, u_{i+1})$ si alguna de las funciones base $N_{j,p-1}(u)$ y $N_{j+1,p-1}(u)$ no lo son, luego:
                    \begin{itemize}
                        \item $N_{j,p-1}(u)$ es no nula a lo sumo cuando $j\in \{i-p+1, i-p+2, \dots ,i\}$
                        \item $N_{j+1,p-1}(u)$ es no nula a lo sumo cuando $j\in \{i-p, i-p+1, \dots ,i-1\}$                 
                    \end{itemize}
                    Luego $N_{j,p}$ es no nula cuando $j\in [i-p, i]$, probando así las posibles $p+1$ funciones base no nulas con sus indices posibles correspondientes.
                \end{itemize}
                %Finalmente para probar la cantidad de funciones base nulas en un intervalo $[u_i, u_{i+1})$, suponemos que $u_j=u_{j+1}=\dots=u_{j+k-1}$, 
                %ahora tenemos que una función base $\mathbf{N_{j, p}(u)}$ será activa en $[u_i, u_{i+1})$ si su soporte $\mathbf{(u_j, u_{j+p+1})}$ interseca a este, notamos que $\forall j \in [i-p, i-p+k-1]$, $\mathbf{N_{j, p}(u)}$ tiene soporte local que llega hasta $u_i$, luego su soporte no interseca con el intervalo, dandonos así $k$ funciones nulas y por ende, $p+1-k$ funciones base activas.
                
                Para demostrar la reducción, consideramos un nudo de valor $\mathbf{u^*}$ con multiplicidad $k$, tal que la secuencia de nudos coincidentes es $\mathbf{u_j=u_{j+1}=\dots=u_{i}}$, donde $\mathbf{i=j+k-1}$.

                Una función base $\mathbf{N_{m, p}(u)}$ es nula en el intervalo $[u_i, u_{i+1})$ si su soporte $\mathbf{(u_m, u_{m+p+1})}$ no interseca dicho intervalo, es decir, si $\mathbf{u_{m+p+1} \leq u_i}$.

                Las $\mathbf{k}$ funciones base con los índices más bajos en el rango máximo, dadas por el conjunto\\
                 $\mathbf{J_{\text{nulo}} = \{i-p, i-p+1, \dots, i-p+k-1\}}$, se anulan.
                Analizamos el índice final del soporte para el último elemento de este conjunto, $\mathbf{m = i-p+k-1}$:
                $$\mathbf{u_{m+p+1}} = u_{(i-p+k-1) + p + 1} = \mathbf{u_{i+k}}$$.
                Puesto que $\mathbf{u_i, u_{i+1}, \dots, u_{i+k-1}}$ son todos iguales a $\mathbf{u^*}$, la secuencia de $k$ nudos coincidentes fuerza a estas $\mathbf{k}$ bases a ser nulas. Por lo tanto, el número de funciones base nulas es $\mathbf{k}$. 
                Consecuentemente, el número de funciones base activas o no nulas es:
                $\mathbf{p+1} - \mathbf{k}$.
                %\newpage
            \item \textbf{Partición de la Unidad}:
                \begin{itemize}
                    \item \textbf{Caso Base ($p=0$)}: como hemos probado en el apartado anterior en $p=0$ por cada intervalo $[u_i, u_{i+1})$ hay una sola función base no nula, luego $\forall u \in [u_0, u_m), \, \exists j\in R^+$ tal que $u\in [u_j,u{j+1})$, luego,
                    \begin{equation*}
                        \sum_{i=0}^{n} N_{i,0}(u) = N_{j,0}(u) = 1.
                    \end{equation*}
                    \item \textbf{Paso de inducción sobre $p$}: asumimos ahora que se cumple $\sum_{i=0}^{n} N_{i,p}(u)=1$, empezamos entonces con el algoritmo de Cox-de Boor \ref{alg:Cox} talque,
                    %\begin{align*}
                        %\sum_{i=0}^{n} N_{i,p}(u) %=& \sum_{i=0}^{n} \left[\frac{u - u_i}{u_{i+p} - u_i} N_{i,p-1}(u) + \frac{u_{i+p+1} - u}{u_{i+p+1} - u_{i+1}} N_{i+1,p-1}(u)\right]=\\
                                                 % =& \sum_{i=0}^{n} \left[ \frac{u - u_i}{u_{i+p} - u_i} N_{i,p-1}(u) \right] + \sum_{i=0}^{n} \left[ \frac{u_{i+p+1} - u}{u_{i+p+1} - u_{i+1}} N_{i+1,p-1}(u) \right]=\\
                                                  %=& \sum_{i=0}^{n} \left[ \frac{u - u_i}{u_{i+p} - u_i} N_{i,p-1}(u) \right] + \sum_{i=1}^{n+1} \left[ \frac{u_{i+p} - u}{u_{i+p} - u_{i}} N_{i,p-1}(u) \right].
                                                  
                    %\end{align*}
                    \begin{align*}
                        \sum_{i=0}^{n} N_{i,p}(u) =& \sum_{i=0}^{n} \left[\frac{u - u_i}{u_{i+p} - u_i} N_{i,p-1}(u) + \frac{u_{i+p+1} - u}{u_{i+p+1} - u_{i+1}} N_{i+1,p-1}(u)\right]=\\
                                                  =& \sum_{i=0}^{n} \left[ \frac{u - u_i}{u_{i+p} - u_i} N_{i,p-1}(u) \right] + \sum_{i=0}^{n} \left[ \frac{u_{i+p+1} - u}{u_{i+p+1} - u_{i+1}} N_{i+1,p-1}(u) \right]=\\
                                                  =& \sum_{i=0}^{n} \left[ \frac{u - u_i}{u_{i+p} - u_i} N_{i,p-1}(u) \right] + \sum_{i=1}^{n+1} \left[ \frac{u_{i+p} - u}{u_{i+p} - u_{i}} N_{i,p-1}(u) \right].
                    \end{align*}
                    Tomando que el soporte de $N_{i,p-1}(u)$ es $[u_i,u_i+p)$ y que $U$ es abierto, luego $u_0=u_1=\dots = u_p$, por lo que $N_{0,p-1}(u)$, al tener soporte degenerado, es siempre nula, y $N_{n+1,p-1}$ es nula $\forall u \in [u_0, u_n]$ nos queda:
                    \begin{align*}
                        \sum_{i=0}^{n} N_{i,p}(u) =& \sum_{i=0}^{n} \left[ \frac{u - u_i}{u_{i+p} - u_i} N_{i,p-1}(u) \right] + \left[ \frac{u_{i+p} - u}{u_{i+p} - u_{i}} N_{i,p-1}(u) \right] =\\
                                                  =& \sum_{i=0}^{n} \left[ \frac{u - u_i}{u_{i+p} - u_i} + \frac{u_{i+p} - u}{u_{i+p} - u_{i}}  \right]N_{i,p-1}(u) = \sum_{i=0}^{n}N_{i,p-1}(u) = 1.
                    \end{align*}
                \end{itemize}
            
            \item \textbf{Invarianza bajo Transformaciones Afines}: sea una transformación afín $T(\mathbf{P}) = \mathbf{M} \mathbf{P} + \mathbf{t}$, aplicamos esta a la curva obteniendo:
            \begin{align*}
                    T(\mathbf{C}(u)) &= \mathbf{M} \mathbf{C}(u) + \mathbf{t} = \mathbf{M} \left( \sum_{i=0}^{n} N_{i,p}(u) \mathbf{P}_i \right) + \mathbf{t} =\\
                                     &=  \sum_{i=0}^{n} \mathbf{M} (N_{i,p}(u) \mathbf{P}_i) + 1 \cdot \mathbf{t} = \sum_{i=0}^{n} N_{i,p}(u) (\mathbf{M} \mathbf{P}_i) + \mathbf{t} \left( \sum_{i=0}^{n} N_{i,p}(u) \right)=\\
                                     &= \sum_{i=0}^{n} N_{i,p}(u) (\mathbf{M} \mathbf{P}_i + \mathbf{t}).
            \end{align*}

            Siendo esta última serie la curva generada por los puntos de control transformados mediante la transformación afín.
            
            \item \textbf{Continuidad Controlable}: Suponemos que $u_j$ tiene multiplicidad $k \leq p$, aplicamos ahora la definición de la derivada de la curva:
            \begin{equation*}
                \mathbf{C}'(u) = \frac{d}{du} \left( \sum_{i=0}^{n} N_{i,p}(u) \mathbf{P}_i \right) = \sum_{i=0}^{n} N_{i,p}'(u) \mathbf{P}_i = \sum_{i=0}^{n} \left[ \frac{p}{u_{i+p} - u_i} N_{i,p-1}(u) - \frac{p}{u_{i+p+1} - u_{i+1}} N_{i+1,p-1}(u) \right] \mathbf{P}_i.
            \end{equation*}
            Reordenamos ahora los indices del término de la derecha de la suma, aplicando las mismas propiedades que en el apartado anterior, obteniendo así:
            \begin{equation*}
                \mathbf{C}'(u) = \sum_{i=0}^{n-1} N_{i,p-1}(u) \underbrace{\left[ p \frac{\mathbf{P}_{i+1} - \mathbf{P}_i}{u_{i+p+1} - u_{i+1}} \right]}_{\mathbf{P}_i^{(1)}}  = \sum_{i=0}^{n-1} N_{i,p-1}(u) \mathbf{P}_i^{(1)}.
            \end{equation*}
            Así, de manera recursiva definimos la derivada $r$-ésima de la curva:
            \begin{equation*}
                \mathbf{C}^{(r)}(u) = \sum_{i} N_{i, p-r}(u) \mathbf{P}_i^{(r)}, \quad \left(\mathbf{P}_i^{(r)} = p \frac{\mathbf{P}_{i+1}^{(r-1)} - \mathbf{P}_i^{(r-1)}}{u_{i+p+1} - u_{i+1}}\right)
            \end{equation*}
            Obteniendo así que para $r=p-k$, si $u=u_j$, $N_{j-1,k}=1$, luego:
            \begin{align*}
                \lim_{u \to u_j^-} \mathbf{C}^{(r)}(u) &= \sum_{i} \mathbf{P}_i^{(r)} \lim_{u \to u_j^-} N_{i, k}(u) = \mathbf{P}_{j-1}^{(r)} \cdot \lim_{u \to u_j^-} N_{j-1, k}(u) = \mathbf{P}_{j-1}^{(r)}.\\
                \lim_{u \to u_j^+} \mathbf{C}^{(r)}(u) &= \sum_{i} \mathbf{P}_i^{(r)} \lim_{u \to u_j^+} N_{i, k}(u) = \mathbf{P}_{j-1}^{(r)} \cdot \lim_{u \to u_j^+} N_{j-1, k}(u) = \mathbf{P}_{j-1}^{(r)}.
            \end{align*}
            Para $r=p-k+1$, tenemos que:
            \begin{align*}
                &\lim_{u \to u_j^-} N_{j-1, k-1}(u) = 1, \, \lim_{u \to u_j^+} N_{j-1, k-1}(u) = 0.\\
                &\lim_{u \to u_j^-} N_{j, k-1}(u) = 0, \, \lim_{u \to u_j^+} N_{j, k-1}(u) = 1.\\
            \end{align*}
            Por lo que en este caso:
             \begin{align*}
                \lim_{u \to u_j^-} \mathbf{C}^{(r)}(u) &= \sum_{i} \mathbf{P}_i^{(r)} \lim_{u \to u_j^-} N_{i, k-1}(u) = \mathbf{P}_{j-1}^{(r)} \cdot \lim_{u \to u_j^-} N_{j-1, k-1}(u) = \mathbf{P}_{j-1}^{(r)}.\\
                \lim_{u \to u_j^+} \mathbf{C}^{(r)}(u) &= \sum_{i} \mathbf{P}_i^{(r)} \lim_{u \to u_j^+} N_{i, k-1}(u) = \mathbf{P}_{j}^{(r)} \cdot \lim_{u \to u_j^+} N_{j1, k-1}(u) = \mathbf{P}_{j}^{(r)}.
            \end{align*}
            Luego como la continuidad de $\mathbf{C}^{(r)}(u)$ depende de los puntos de control, $\mathbf{C}(u)\notin L^{p-k+1}(\Omega)$. 
        \end{enumerate}
\end{proof}
\subsection{Igualdad de las nuevas funciones base bajo refinamiento}
\label{sec:dem_new_bases}
\begin{proof}
    Procederemos mediante inducción sobre $p$:
    \begin{itemize}
        \item \textbf{Caso base ($p=0$)}: Sea $\xi$ con valor dentro del intervalo de nudos $[u_k, u_{k+1})$, separamos los casos:
        \begin{itemize}
            \item $i < k$: En este caso $N_{i,0}(u)$ comparte soporte con $N^*_{i,0}(u)$.
            \item $i > k$: En este caso $N_{i,0}(u)$ comparte soporte con $N^*_{i+1,0}(u)$.
            \item $i = k$: En este caso $N_{k,0}(u)$ tiene como soporte a la unión de ambos soportes de $N^*_{k,0}(u)$ y $N^*_{k+1,0}(u)$, obteniendo la igualdad:
            \begin{align*}
                &N_{k,0}(u) = 1_{[u_k, u_{k+1})}(u) = 1_{[u_k, \xi)}(u) + 1_{[\xi, u_{k+1})}(u) =\\
                =&N^*_{k,0}(u) + N^*_{k+1,0}(u) = \alpha_k N^*_{k,0}(u) +(1-\alpha_{k+1}) N^*_{k+1,0}(u).
            \end{align*}
        \end{itemize}
        \item \textbf{Paso de inducción sobre $p$}: inicamos asumiendo cierta la hipótesis para $p-1$ y sustituimos en el algoritmo de Cox-de Boor,
            \begin{align*}
                N_{i,p}(u) &= \frac{u - u_i}{u_{i+p} - u_i} \left[ \alpha_{i,p-1} N^*_{i,p-1}(u) + (1 - \alpha_{i+1,p-1}) N^*_{i+1,p-1}(u) \right]+ \\
                        &+ \frac{u_{i+p+1} - u}{u_{i+p+1} - u_{i+1}} \left[ \alpha_{i+1,p-1} N^*_{i+1,p-1}(u) + (1 - \alpha_{i+2,p-1}) N^*_{i+2,p-1}(u) \right].
            \end{align*}
            Donde $$\alpha_{i,p} = 
            \begin{cases} 
            1 & \text{si } i \le k - p ,\\
            \frac{\xi - u_i}{u_{i+p} - u_i} & \text{si } k - p < i \le k ,\\
            0 & \text{si } i > k.
            \end{cases}$$
            Reorganizamos ahora la igualdad agrupando los términos asociados a cada función base distinta:
            \begin{itemize}
                \item Término asociado a $N^*_{i,p-1}$: $$\frac{u - u_i}{u_{i+p} - u_i} \alpha_{i,p-1} = \alpha_{i,p} \frac{u - u^*_i}{u^*_{i+p} - u^*_i}.$$
                \item Término asociado a $N^*_{i+1,p-1}$: $$\frac{u - u_i}{u_{i+p} - u_i}(1 - \alpha_{i+1,p-1}) + \frac{u_{i+p+1} - u}{u_{i+p+1} - u_{i+1}}\alpha_{i+1,p-1} = \alpha_{i,p} \frac{u^*_{i+p+1} - u}{u^*_{i+p+1} - u^*_{i+1}} + (1 - \alpha_{i+1,p}) \frac{u - u^*_{i+1}}{u^*_{i+1+p} - u^*_{i+1}}.$$
                \item Término asociado a $N^*_{i+2,p-1}$: $$\frac{u_{i+p+1} - u}{u_{i+p+1} - u_{i+1}} (1 - \alpha_{i+2,p-1}) = (1 - \alpha_{i+1,p}) \frac{u^*_{i+p+2} - u}{u^*_{i+p+2} - u^*_{i+2}}.$$
            \end{itemize}
            Sustituyendo ahora en la igualdad original obtenemos:
            \begin{align*}
                N_{i,p}(u) &= \alpha_{i,p} \underbrace{\left[ \frac{u - u^*_i}{u^*_{i+p} - u^*_i} N^*_{i,p-1} + \frac{u^*_{i+p+1} - u}{u^*_{i+p+1} - u^*_{i+1}} N^*_{i+1,p-1} \right]}_{N^*_{i,p}(u)}+ \\
                        &+ (1 - \alpha_{i+1,p}) \underbrace{\left[ \frac{u - u^*_{i+1}}{u^*_{i+1+p} - u^*_{i+1}} N^*_{i+1,p-1} + \frac{u^*_{i+p+2} - u}{u^*_{i+p+2} - u^*_{i+2}} N^*_{i+2,p-1} \right]}_{N^*_{i+1,p}(u)}.
            \end{align*}
    \end{itemize}
\end{proof}
\subsection{Equivalencia de curvas bajo refinamiento}
\label{sec:refinement_proof}
\begin{proof} Separaremos entre indices donde $\alpha_j$ toma valores 0 o 1 y en los que no son tan triviales
        \begin{itemize}
            
        \item Para los indices $j\leq k-p$,\, $\alpha_j=1, \, \mathbf{Q}_{j}^{w} = (1) \mathbf{P}_{j}^{w} + (1 - 1) \mathbf{P}_{j-1}^{w} =  \mathbf{P}_{j}^{w}$, $N_{j,p}=N^*_{j,p}$.

        \item Ahora para los indices $j > k$,\, $\alpha_j=0, \, \mathbf{Q}_{j}^{w} = (0) \mathbf{P}_{j}^{w} + (1 - 0) \mathbf{P}_{j-1}^{w} =  \mathbf{P}_{j-1}^{w}$, $N_{j-1,p}=N^*_{j,p}$.
    
        \item Por último para los indices $i \in \{k-p, k-p+1, \dots, k\}$, obtenemos la definición de la influencia de estos indices mediante:
        \begin{equation*}
            \mathbf{C}^{w}(u) = \sum_{i=k-p}^{k} N_{i,p}(u) \mathbf{P}_{i}^{w}.
        \end{equation*}
        descomponemos ahora las funciones base en las nuevas funciones base:
        \begin{equation*}
            \mathbf{C}^{w}(u) = \sum_{i=k-p}^{k} [\alpha_{i} N_{i,p}^*(u) + (1 - \alpha_{i+1}) N_{i+1,p}^*(u)] \mathbf{P}_{i}^{w}.
        \end{equation*}
        Separamos en las suma en sus dos términos principales y reindexamos:
        \begin{align*}
            &\sum_{i=k-p}^{k} \alpha_{i} N_{i,p}^*(u) \mathbf{P}_{i}^{w}.\\
            &\sum_{i=k-p}^{k} (1 - \alpha_{i+1}) N_{i+1,p}^*(u) \mathbf{P}_{i}^{w}=\sum_{j=k-p+1}^{k+1} (1 - \alpha_j) N_{j,p}^*(u) \mathbf{P}_{j-1}^{w}.
        \end{align*}
        Como hemos mostrado anteiormente la equivalencia de los términos de la serie para $j=k-p, \, j=k+1$ y ambos términos aparecen con su función base y punto de control correspondiente una única vez, podemos eliminarlos de la suma y centrarnos en los indices de los que desconocemos si se cumple.
        \begin{align*}
            &\mathbf{C}^{w}(u) = \sum_{j=k-p+1}^{k} [\alpha_{j} N_{j,p}^*(u)\mathbf{P}_{j}^{w} + (1 - \alpha_j) N_{j,p}^*(u)] \mathbf{P}_{j-1}^{w}=\\
            &=\sum_{j=k-p+1}^{k} N_{j,p}^*(u)\left[\alpha_i \mathbf{P}_{i}^{w} + (1 - \alpha_i) \mathbf{P}_{i-1}^{w} \right]=\sum_{j=k-p+1}^{k} N_{j,p}^*(u) \mathbf{Q}_{j}^{w}.
        \end{align*}
        
        \end{itemize}
    \end{proof}

\section{Código omitido}

\subsection{FEM}
\label{sec:codigo_FEM}
\textbf{Código de las funciones base FEM}
\begin{listing}[H]
\inputminted[
    linenos,
    breaklines,
    breakanywhere,
    frame=lines,
    fontsize=\footnotesize
]{octave}{FEMbasis.m}
\end{listing}
\subsection{Figuras de Bézier}
\label{sec:codigo_bezier}
\textbf{Código de las bases de Bernstein}
\begin{listing}[H]
\inputminted[
    linenos,
    breaklines,
    frame=lines,
    fontsize=\footnotesize
]{octave}{plot_bernstein_bases.m}
\end{listing}
\newpage
\textbf{Código de las curvas racionalizadas de Bernstein}
\inputminted[
    linenos,
    breaklines,
    breakanywhere,
    frame=lines,
    fontsize=\footnotesize
]{octave}{RatBezier3d.m}
\subsection{Código fundamental para el funcionamiento en C++}
\label{sec:codigo_fund}
\textbf{Clase iMatrix}
\inputminted[
    frame=lines,
    framesep=2mm,
    linenos=true,
    style=dracula,
    bgcolor=black, 
    fontfamily=tt,
    tabsize=4,
    fontsize=\tiny
]{c++}{iMatrix.cpp}

\textbf{Fichero funciones.hpp}
\inputminted[
    frame=lines,
    framesep=2mm,
    linenos=true,
    style=dracula,
    bgcolor=black, 
    fontfamily=tt,
    tabsize=4,
    fontsize=\tiny
]{c++}{funciones.hpp}

\textbf{Fichero funciones.cpp}
\inputminted[
    frame=lines,
    framesep=2mm,
    linenos=true,
    style=dracula,
    bgcolor=black, 
    fontfamily=tt,
    tabsize=4,
    fontsize=\tiny
]{c++}{funciones.cpp}

\subsection{Curvas de NURBS}
\label{sec:codigo_NURBS1}
\textbf{Código del dibujo de las curvas de NURBS}
\inputminted[
    linenos,
    breaklines,
    breakanywhere,
    frame=lines,
    fontsize=\footnotesize
]{octave}{CurvePlotter.m}

\textbf{Código de las derivadas de las funciones base de NURBS}
\begin{minted}[
    frame=lines,          
    framesep=2mm,         
    linenos=true,         
    style=dracula,      
    bgcolor=black,
    fontfamily=tt,        
    tabsize=4,            
    fontsize=\footnotesize 
]{c++}
int p=3;
std::vector<double> Nurbs={0,0,0,0,0.5,1,1,1,1};
std::vector<double> weights={1,1,5,1,1};

double N_Steps = 1000;
std::vector<double> eval(Nurbs.size()-p);
iMatrix<double> ders(p+1,p+1);
iMatrix<double> sol(Nurbs.size()-p,N_Steps+1);
//std::vector<double> Nurb(NurbsVector.size());
double Step=1/(N_Steps);
auto start = std::chrono::high_resolution_clock::now();
for(unsigned int i = 0; i<N_Steps+1; i++){
    
    double t=Step*i;
    //std::cout << "Iteración iniciada " << i<< std::endl;
    int span = FindSpan(Nurbs, t, p, Nurbs.size()-2-p);
    ders = RatDersBasisFuns(span,t,p,1,Nurbs, weights);
    ders.PrintMatrix();
    std::cout<<"---------------------"<<std::endl;
    for(int j = span-p; j<=span;j++){
        if(j>=0) {
            sol(j,i)=ders(1,j-span+p);
        }
        else{
            //Caso error.
        }
        
    }
}

Write_BasisFunctions(sol, "path+name");
auto end = std::chrono::high_resolution_clock::now();
auto duration = std::chrono::duration_cast<std::chrono::nanoseconds>(end - start);

std::cout << "Tiempo tomado: " << duration.count() << " nanosegundos\n";
\end{minted}

\textbf{Código de las funciones base de NURBS}
\begin{minted}[
    frame=lines,          
    framesep=2mm,         
    linenos=true,         
    style=dracula,      
    bgcolor=black,
    fontfamily=tt,        
    tabsize=4,            
    fontsize=\footnotesize 
]{c++}
int p=3;
double N_Steps = 1000;
std::vector<double> KnotVector={0,0,0,0,0.5,1,1,1,1};
std::vector<double> WeightVector={1,1,5,1,1};
iMatrix<double> sol(KnotVector.size()-p-1,N_Steps+1);

double Step=1/(N_Steps);
for(unsigned int i = 0; i<N_Steps+1; i++){
    double t=Step*i;
    int span = FindSpan(KnotVector, t, p, KnotVector.size()-2-p);
    std::vector<double> eval= RationalBasisFuns(span, t , p, KnotVector, WeightVector);
    for(int j = span-p; j<=span;j++){
        if(j>=0) {
            sol(j,i)=eval[j-span+p];                
        }
        else{
            //Caso error.
        }        
    } 
}
Write_BasisFunctions(sol, "path+name");
\end{minted}

\subsection{Superficies de NURBS}
\label{sec:codigo_NURBS2}
\textbf{Código del dibujo de las superficies de NURBS}
\begin{minted}[
    linenos,           
    breaklines,        
    breakanywhere,     
    frame=lines,       
    fontsize=\footnotesize 
]{octave}
dataX =  dlmread('C:\Users\carlo\OneDrive\Escritorio\Uni\TFG\DataFiles\Nurbs\Superficies\SuperficieRationalEj2X.txt', ' ');
dataY =  dlmread('C:\Users\carlo\OneDrive\Escritorio\Uni\TFG\DataFiles\Nurbs\Superficies\SuperficieRationalEj2Y.txt', ' ');
dataZ =  dlmread('C:\Users\carlo\OneDrive\Escritorio\Uni\TFG\DataFiles\Nurbs\Superficies\SuperficieRationalEj2Z.txt', ' ');

hold on
surf(dataX, dataY, dataZ);

dataCtrlX =  dlmread('C:\Users\carlo\OneDrive\Escritorio\Uni\TFG\DataFiles\Nurbs\CtrlPts\Ej2X.txt', ' ');
dataCtrlY =  dlmread('C:\Users\carlo\OneDrive\Escritorio\Uni\TFG\DataFiles\Nurbs\CtrlPts\Ej2Y.txt', ' ');
dataCtrlZ =  dlmread('C:\Users\carlo\OneDrive\Escritorio\Uni\TFG\DataFiles\Nurbs\CtrlPts\Ej2Z.txt', ' ');

[n1 n2] = size(dataCtrlX);

for i=1:n1
  plot3(dataCtrlX(i,:),dataCtrlY(i,:),dataCtrlZ(i,:), color='r');
endfor

for j=1:n2
  plot3(dataCtrlX(:,j),dataCtrlY(:,j),dataCtrlZ(:,j), color='r');
endfor

scatter3(dataCtrlX(:), dataCtrlY(:), dataCtrlZ(:), 30, 'k', 'filled', 'MarkerEdgeColor', 'w');
view(30, 45);
grid on;

print -dsvg 'Superficie_NURBS_Ej1.svg';
hold off;
\end{minted}

\textbf{Código del cálculo de las superficies de NURBS}
\begin{minted}[
    frame=lines,          
    framesep=2mm,         
    linenos=true,         
    style=dracula,      
    bgcolor=black,
    fontfamily=tt,        
    tabsize=4,            
    fontsize=\footnotesize 
]{c++}
std::vector<iMatrix<double>> CtrlPts = ReadDataFile_CtrlPts("path+nameCtrlPts");
iMatrix<double> Weights =ReadDataFile_Matrix("path+nameWeights");
std::vector<iMatrix<double>> CtrlPtsW=WeightCtrlPtsSurf(CtrlPts,Weights);
std::vector<double> V1={0,0,0,0.5,0.5,1,1,1};
std::vector<double> V2={0,0,0,0,1,1,1,1};
int p1=2;
int p2=3;
double N_Steps_1 = 25;
double N_Steps_2 = 25;

double Step_1=1/N_Steps_1;
double Step_2=1/N_Steps_2;

std::vector<iMatrix<double>> eval(N_Steps_1+1);
for(unsigned int i=0; i<N_Steps_1+1;i++){
    
    iMatrix<double> auxMatrix(3,N_Steps_2+1);
    for(unsigned int j=0; j< N_Steps_2+1;j++){
        
        std::vector<double> aux = SurfacePointRational(V1.size()-2-p1,p1,V1, V2.size()-2-p2,p2,V2,CtrlPtsW,i*Step_1,j*Step_2);
        for(unsigned int k=0; k<3;k++){
            auxMatrix(k,j)=aux[k];
        }
    }
    eval[i]=auxMatrix;
}

Write_BezierSurface(eval,"path+nameSurf" );
Write_CtrlPts(CtrlPts,"path+nameCtrlPts");
\end{minted}

\end{document}